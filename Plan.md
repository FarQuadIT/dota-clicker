# 🎮 План разработки системы уровней - Dota Clicker

## 🎯 Общая цель
Создать простую систему уровней героя (1-30) где один уровень = одна волна крипов + босс. Прошел уровень - повысил уровень героя, не прошел - остался на том же.

---

## 📋 Фаза 1: Добавление новых типов крипов

### ✅ Микрошаг 1.1 - Расширение AssetsManager для новых крипов
- [x] **Задача для AI**: Обновить `src/game/managers/AssetsManager.ts`
  - [x] Добавить в манифест (метод `createAssetManifest`) новых крипов:
    - **medved**: attack(5x5, 21 кадр), death(5x5, 21 кадр), idle(5x6, 27 кадров)
    - **satyr**: attack(5x5, 23 кадра), death(5x6, 30 кадров), idle(6x7, 40 кадров)  
    - **shishka**: attack(5x6, 27 кадров), death(5x6, 27 кадров), idle(5x5, 24 кадра)
    - **voul**: attack(5x5, 25 кадров), death(4x8, 31 кадр), idle(7x5, 34 кадра)
    - **wolf**: attack(3x7, 21 кадр), death(3x6, 18 кадров), idle(5x5, 23 кадра)
  - [x] Путь к файлам: `/media/game/assets/creeps/`
  - [x] frameWidth и frameHeight нужно подобрать по аналогии с direCreep

### ✅ Микрошаг 1.2 - Обновление Creep класса
- [x] **Задача для AI**: Обновить `src/game/entities/Creep.ts`
  - [x] Добавить параметр `creepType: string` в конструктор
  - [x] Изменить вызов `assetsManager.getCreepFrames('direCreep', ...)` на использование переданного типа
  - [x] Поддержка всех 6 типов: direCreep, medved, satyr, shishka, voul, wolf
  - [x] Сохранить адаптивное позиционирование

### ✅ Микрошаг 1.3 - Тестирование одного нового крипа
- [x] **Задача для AI**: Обновить GameController для тестирования
  - [x] В методе `createNewCreep()` передать другой тип крипа в конструктор (например 'medved')
  - [x] Проверить что анимации работают корректно
  - [x] Убедиться что позиционирование адаптивное

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 1:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 1:**
- [x] Запустить игру `/game`
- [x] Вместо стандартного direCreep должен появиться новый крип (wolf)
- [x] Новый крип должен иметь анимации idle, attack, death
- [x] Крип корректно позиционируется и движется к герою
- [x] При клике герой атакует нового крипа и он умирает
- **✅ Если все работает - Фаза 1 завершена успешно!**

---

## 📋 Фаза 2: Рандомизация крипов

### ✅ Микрошаг 2.1 - Простая рандомизация
- [x] **Задача для AI**: Обновить GameController для рандомного выбора крипов
  - [x] Добавить массив доступных типов крипов в класс GameController
  - [x] В методе `createNewCreep()` случайно выбирать тип из массива
  - [x] Проверить что все типы крипов спавнятся корректно

### ✅ Микрошаг 2.2 - Конфигурация типов крипов
- [x] **Задача для AI**: Создать `src/game/config/creepsConfig.ts` (файл уже есть, но пустой)
  - [x] Добавить конфигурацию `CREEP_TYPES` с параметрами каждого типа:
    ```typescript
    export interface CreepTypeConfig {
      scale: number;
      health: number;
      speed: number;
    }
    
    export const CREEP_TYPES: Record<string, CreepTypeConfig> = {
      direCreep: { scale: 1.0, health: 100, speed: 2 },
      medved: { scale: 1.2, health: 150, speed: 1.5 },
      satyr: { scale: 0.9, health: 80, speed: 2.5 },
      // и т.д. для всех типов
    }
    ```
  - [x] Использование конфигурации при создании крипов в GameController

### ✅ Микрошаг 2.3 - Тестирование всех крипов
- [x] **Задача для AI**: Убедиться что работают все крипы
  - [x] Протестировать каждый тип крипа по отдельности
  - [x] Проверить анимации attack, death, idle для всех
  - [x] Убедиться в корректном масштабировании

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 2:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 2:**
- [x] Запустить игру `/game`
- [x] Перезагружать страницу несколько раз - каждый раз должен появляться случайный крип
- [x] Должны появляться все 6 типов: direCreep, medved, satyr, shishka, voul, wolf
- [x] Каждый тип крипа должен иметь свой размер согласно конфигурации
- [x] Все крипы корректно анимируются и реагируют на атаки героя
- [x] **ДОПОЛНИТЕЛЬНО**: Выполнить `testAllCreeps()` в консоли для проверки конфигурации
- **✅ Если рандомизация работает и все крипы отображаются - Фаза 2 завершена!**

---

## 📋 Фаза 2.5: Интеграция характеристик из старого проекта

### ✅ Микрошаг 2.5.1 - Настройка героя и загрузка характеристик
  - [x] **Задача для AI**: Расширение HeroStats и интеграция с реальными данными
    - [x] **ПРОБЛЕМА**: В `HeroStats` нет полей `level` и `coins`, но они есть в `RawHeroData` с сервера
    - [x] **РЕШЕНИЕ**: Добавить в `HeroStats` недостающие поля:
    ```typescript
    /** Текущий уровень героя */
    "level": number;
    
    /** Текущее количество золота */
    "coins": number;
    
    /** Текущее здоровье героя (не максимальное) */
    "current-health": number;
    
    /** Текущая мана героя (не максимальная) */
    "current-mana": number;
    ```
  - [x] **Обновление mappers.ts**: Добавить маппинг новых полей в `mapHeroData()`:
    ```typescript
    "level": data.level ?? 1,
    "coins": data.coins ?? 0,
    "current-health": data.maxHealth ?? 100, // Изначально = максимальному
    "current-mana": data.maxEnergy ?? 50,    // Изначально = максимальной
    ```
  - [x] **Интеграция с GameController**:
    - Добавить в `GameController` метод `getHeroStats()` для получения характеристик из heroStore
    - Урон героя должен браться из `stats["damage"]` вместо константы
    - HP и мана героя должны отслеживаться в реальном времени
  - [x] **Убрать инициализацию базовыми данными** - теперь данные берутся с сервера через App.tsx

### ✅ Микрошаг 2.5.1+ - Визуальные полоски HP/MP над героем
  - [x] **Задача для AI**: Создать визуальные полоски здоровья и маны над джаггернаутом
    - [x] **Изучить старый проект**: В `old_project_game/front1/game/heroes/hero.js` метод `drawBars()` показывает как рисовались полоски
    - [x] **Современное решение**: Использовать PixiJS Graphics вместо Canvas 2D
    - [x] **Создать компонент**: `src/game/components/HeroHealthBar.ts` с классом для отображения полосок
    - [x] **Интеграция с Hero.ts**: Добавить полоски как дочерние элементы героя
    - [x] **Особенности из старого проекта**:
    ```typescript
    // Полоска здоровья (красная)
    - Фон: rgba(255, 0, 0, 0.8) или rgba(66, 102, 0, 0.8) при отсутствии регена
    - Заполнение: #ff4d4d или #88cc00
    - Текст: "current/max" по центру белым цветом
    
    // Полоска маны (синяя) 
    - Фон: rgba(0, 0, 255, 0.8) или rgba(0, 0, 41, 0.8) при отсутствии регена
    - Заполнение: #00bfff
    - Текст: "current/max" по центру белым цветом
    - Позиция: под полоской здоровья с отступом 5px
    ```
  - [x] **Плавная анимация**: Как в старом проекте - плавное изменение полосок при изменении HP/MP
  - [x] **Адаптивность**: Полоски должны масштабироваться с героем и быть всегда над ним
  - [x] **Синхронизация с heroStore**: Полоски должны отображать значения из `stats["current-health"]`, `stats["current-mana"]`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.1:**
**Что должно работать после выполнения микрошага 2.5.1:**
- [x] **Консоль браузера**: `heroStore.getState().stats` должно показать ВСЕ 12 полей
- [x] **MainPage `/`**: Должны отображаться новые поля `level`, `coins`, `current-health`, `current-mana`
- [x] **Инициализация**: Все характеристики должны иметь базовые значения из старого проекта
- [x] **Интеграция**: GameController должен получать урон героя из `heroStore.stats["damage"]` (пока еще не используется, но метод есть)
- [x] **НЕТ ошибок** в консоли браузера связанных с типами или отсутствующими полями

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.1+:**
**Что должно быть ВИЗУАЛЬНО видно после выполнения микрошага 2.5.1+:**
- [x] **Игровая страница `/game`**: Над джаггернаутом должны отображаться 2 полоски:
  - **Красная полоска HP** с текстом "100/100" (или текущие значения)
  - **Синяя полоска MP** снизу с текстом "50/50" (или текущие значения)
- [x] **Адаптивность**: При изменении размера окна полоски должны оставаться над героем
- [x] **Плавная анимация**: Полоски должны плавно изменяться при изменении HP/MP
- [x] **Синхронизация**: Значения в полосках должны соответствовать `heroStore.stats["current-health"]` и `stats["current-mana"]`
- [x] **Стиль как в старом проекте**: Цвета и размеры должны быть похожи на оригинал
- [x] **Исправлен баг с заполнением**: Полоски сразу отображаются полными без анимации заполнения
- [x] **Исправлен баг с характеристиками**: При входе в игру все характеристики обновляются с сервера
- **✅ Микрошаг 2.5.1+ ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.2 - Реализация характеристик и особых умений крипов  
- [ ] **Задача для AI**: Расширение `src/game/config/creepsConfig.ts`
  - [ ] **ПРОБЛЕМА**: В `CreepTypeConfig` только визуальные параметры, нет боевых характеристик
  - [ ] **РЕШЕНИЕ**: Добавить реальные боевые характеристики:
    ```typescript
    interface CreepTypeConfig {
      // ... существующие поля ...
      
      /** Максимальное здоровье крипа */
      maxHealth: number;
      
      /** Урон который наносит крип герою */
      damage: number;
      
      /** Сколько золота дает за убийство */
      goldReward: number;
      
      /** На каком уровне героя открывается крип */
      requiredHeroLevel: number;
      
      /** Особые способности крипа */
      specialAbilities?: string[];
    }
    ```
  - [ ] **Данные из старого проекта** `old_project_game/front1/game/creepData/creepsConfig.js`:
    ```
    direCreep: maxHealth=10, damage=3, goldReward=1, requiredHeroLevel=0
    shishka: maxHealth=15, damage=2, goldReward=1, requiredHeroLevel=1  
    medved: maxHealth=10, damage=1, goldReward=1, requiredHeroLevel=2
    wolf: maxHealth=5, damage=5, goldReward=1, requiredHeroLevel=3
    satyr: maxHealth=7, damage=2, goldReward=1, requiredHeroLevel=4, abilities=["manaburn"]
    voul: maxHealth=10, damage=2, goldReward=1, requiredHeroLevel=5, abilities=["poison"]
    ```
     - [ ] **Особые способности**:
     - `"manaburn"` (satyr): отнимает ману у героя равную урону
     - `"poison"` (voul): отключает регенерацию HP героя на 1 секунду

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.2:**
**Что должно работать после выполнения микрошага 2.5.2:**
- [x] **Консоль браузера**: `getCreepConfig('direCreep')` должно показать все новые поля (maxHealth, damage, goldReward, requiredHeroLevel)
- [x] **Конфигурация крипов**: Выполнить `testCreepConfigurations()` - все крипы должны спавниться без ошибок
- [x] **Данные из старого проекта**: Проверить что характеристики соответствуют:
  - `direCreep`: maxHealth=10, damage=3, goldReward=1, requiredHeroLevel=0
  - `wolf`: maxHealth=5, damage=5, goldReward=1, requiredHeroLevel=3
  - `satyr`: specialAbilities=["manaburn"]
  - `voul`: specialAbilities=["poison"]
- [x] **НЕТ ошибок** в консоли при загрузке конфигурации крипов
- [x] **Игра работает**: `/game` должна работать как раньше (крипы пока умирают от 1 хита, но конфигурация загружается)
- [x] **Функции тестирования**: Добавлены глобальные функции для консоли браузера
- **✅ Микрошаг 2.5.2 ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.2+ - Визуальные полоски HP крипов
- [x] **Задача для AI**: Создать полоски здоровья для крипов по аналогии с старым проектом
  - [x] **Изучить старый проект**: В `old_project_game/front1/game/creepData/creep.js` метод `drawHealthBar()` показывает как рисовались полоски крипов
  - [x] **Современное решение**: Использовать PixiJS Graphics вместо Canvas 2D
  - [x] **Создать компонент**: `src/game/components/CreepHealthBar.ts` с классом для отображения полоски крипа
  - [x] **Интеграция с Creep.ts**: Добавить полоску как элемент сцены, позиционирующийся относительно крипа
  - [x] **Особенности из старого проекта**:
    ```typescript
    // Полоска здоровья крипа (красная)
    - Размер: barWidth = creepWidth * scale / 3, barHeight = 15
    - Фон: rgba(255, 0, 0, 0.8) (полупрозрачный красный)
    - Заполнение: #ff4d4d (ярко-красный)
    - Текст: "currentHP/maxHP" по центру белым цветом (10px Arial)
    - Позиция: над крипом с настройкой через xBarDelta и yBarDelta из конфигурации
    ```
  - [x] **Адаптивное позиционирование**: 
    ```typescript
    // Координаты из старого проекта:
    const barX = creepX + (creepWidth * scale - barWidth) / 2 + imgConfigs.xBarDelta;
    const barY = creepY + imgConfigs.yBarDelta * creepY;
    ```
  - [x] **Интеграция с конфигурацией**: Использовать параметры `xBarDelta` и `yBarDelta` для каждого типа крипа
  - [x] **Скрытие при смерти**: Полоска НЕ отображается если крип умирает (isDying = true)
  - [x] **Обновление в реальном времени**: Полоска отображает актуальное здоровье крипа и обновляется при нанесении урона
  - [x] **Интеграция с боевой системой**: Крипы теперь имеют реальное здоровье и получают урон через `takeDamage()`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.2+:**
**Что должно быть ВИЗУАЛЬНО видно после выполнения микрошага 2.5.2+:**
- [x] **Игровая страница `/game`**: Над каждым крипом должна отображаться красная полоска здоровья:
  - **Красная полоска HP** с текстом "10/10" (или текущие значения согласно конфигурации)
  - **Правильное позиционирование**: полоска должна быть ровно над крипом
  - **Адаптивность**: при изменении размера окна полоска должна оставаться над крипом
- [x] **Разные типы крипов**: Полоски должны отображаться у всех типов крипов (direCreep, wolf, satyr, etc.)
- [x] **Скрытие при смерти**: Когда крип умирает, полоска должна исчезать
- [x] **Стиль как в старом проекте**: Размеры, цвета и шрифт должны быть похожи на оригинал
- [x] **Синхронизация**: Значения в полоске должны соответствовать реальному здоровью крипа
- [x] **Настройка позиций**: Для разных типов крипов полоски должны позиционироваться по-разному (используя xBarDelta/yBarDelta)
- [x] **Реальная боевая система**: Крипы не умирают от одного удара, имеют здоровье согласно конфигурации
- **✅ Микрошаг 2.5.2+ ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.3 - Реализация атак крипов на героя
- [x] **ПРОБЛЕМА**: Крипы НЕ атакуют героя. В старом проекте крипы наносили урон через `dealDamage(hero)` в определенных кадрах анимации атаки
- [x] **РЕШЕНИЕ**: Реализовать только атаки крипов без регенерации (регенерация будет в следующем шаге)

- [x] **Задача для AI**: Обновление `Hero.ts` и `Creep.ts` для атак крипов
  - [x] **Hero.ts** - добавить методы получения урона:
    ```typescript
    // Новые методы для получения урона (БЕЗ регенерации пока)
    takeDamage(damage: number): void // уменьшает current-health в heroStore
    takeManaburn(damage: number): void // уменьшает current-mana (способность сатира)
    applyPoison(duration: number): void // устанавливает флаг отравления (способность вула)
    
    // Геттеры для состояния
    isAlive(): boolean // проверка что HP > 0
    isPoisoned(): boolean // проверка активен ли яд
    ```

  - [x] **Creep.ts** - добавить атаки героя:
    ```typescript
    // Новые методы для атак героя (аналог старого проекта)
    attackHero(hero: Hero): void // наносит урон герою + особые способности
    canAttackHero(): boolean // проверка может ли атаковать
    
    // Интеграция с анимациями (как в старом проекте)
    private checkAttackFrame(): void // проверяет кадр атаки для нанесления урона
    ```

  - [x] **Изучить старый проект**: Метод `dealDamage(hero)` в `old_project_game/front1/game/creepData/creep.js`:
    ```javascript
    dealDamage(hero) {
      if (hero.health > 0) {
        hero.health = Math.max(0, hero.health - this.creepDamage);
      }
      // Особые способности через functions['attack_modifier']
      if (this.functions['attack_modifier']) {
        this.functions['attack_modifier'](hero);
      }
    }
    ```

  - [x] **Фокус**: Только атаки крипов, регенерация будет в следующем микрошаге

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.3:**
**Что должно работать после выполнения микрошага 2.5.3:**
- [x] **Атаки крипов**: При коллизии крип должен атаковать героя и уменьшать его `current-health` в heroStore
- [x] **Урон от крипов**: Разные типы крипов должны наносить разный урон согласно `creepsConfig.ts`
- [x] **Особые способности крипов**:
  - Satyr: при атаке должен уменьшать `stats["current-mana"]` (manaburn)
  - Voul: должен устанавливать флаг отравления (poison) - но регенерация пока НЕ отключается
- [x] **Синхронизация полосок**: Полоски HP/маны героя должны отображать актуальные значения из heroStore
- [x] **Синхронизация с MainPage**: Изменения HP/маны должны отображаться на `/`
- [x] **Консоль**: `heroStore.getState().stats["current-health"]` должно уменьшаться при получении урона
- [x] **НЕТ регенерации**: HP и мана НЕ восстанавливаются (это будет в следующем микрошаге)
- **✅ Микрошаг 2.5.3 ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.4 - Полная реализация характеристик героя
- [x] **ПРОБЛЕМА**: Многие характеристики героя не работают. Реализованы только полоски HP/маны и получение урона. НЕ работают: регенерация, урон героя, трата маны на атаки, вампиризм
- [x] **РЕШЕНИЕ**: Реализовать ВСЕ базовые характеристики геймплея как в старом проекте

- [x] **Задача для AI**: Комплексное обновление `Hero.ts`, `GameController.ts` и `Creep.ts`

#### **ЧАСТЬ 1: Урон героя по крипам**
  - [x] **ПРОБЛЕМА**: Урон героя НЕ используется в игре. В `GameController.getHeroDamage()` метод уже есть, но крипы получают фиксированный урон
  - [x] **GameController.ts** - исправить метод `dealDamageToCreep()`:
    ```typescript
    // ИСПРАВИТЬ: Должен использовать урон из heroStore.stats["damage"]
    const heroDamage = this.getHeroDamage(); // уже есть!
    const creepDied = this.currentCreep.takeDamage(heroDamage); // уже есть!
    // + добавить трату маны на атаку (см. следующую часть)
    ```

#### **ЧАСТЬ 2: Система маны и атак**
  - [x] **ПРОБЛЕМА**: В старом проекте каждая атака снимала 1 ману. Если мана = 0, была пауза на 2 секунды
  - [x] **GameController.ts** - добавить проверку маны в `dealDamageToCreep()`:
    ```typescript
    // ДОБАВИТЬ проверку маны перед атакой
    public dealDamageToCreep(): void {
      const currentMana = this.hero.getCurrentMana();
      if (currentMana <= 0) {
        // Нет маны - нельзя атаковать
        return;
      }
      
      // Тратим 1 ману на атаку
      this.hero.spendMana(1);
      
      // ... существующий код атаки
    }
    ```

  - [x] **Hero.ts** - добавить методы управления маной:
    ```typescript
    // Новые методы для работы с маной
    spendMana(amount: number): void // тратит ману на атаку
    canAttack(): boolean // проверка есть ли мана для атаки
    ```

#### **ЧАСТЬ 3: Система регенерации**
  - [x] **Hero.ts** - добавить систему регенерации:
    ```typescript
    // Система регенерации (аналог из старого проекта)
    updateRegeneration(deltaTime: number): void // основной метод регенерации
    private regenerateHealth(deltaTime: number): void // восстановление HP
    private regenerateMana(deltaTime: number): void // восстановление маны
    private isRegenerationActive(): boolean // проверка можно ли регенерировать
    
    // Обновление poison системы - теперь отключает регенерацию
    applyPoison(duration: number): void // ОТКЛЮЧАЕТ регенерацию HP на время
    ```

  - [x] **GameController.ts** - интеграция регенерации:
    ```typescript
    // В методе update() добавить
    if (this.hero) {
      this.hero.updateRegeneration(deltaTime);
    }
    ```

#### **ЧАСТЬ 4: Вампиризм**
  - [x] **Hero.ts** - добавить восстановление HP от атак:
    ```typescript
    // Вампиризм - восстановление HP после нанесения урона
    applyVampirism(damageDealt: number): void // восстанавливает HP = vampirism от урона
    ```

  - [x] **GameController.ts** - интеграция вампиризма:
    ```typescript
    // После нанесения урона крипу
    if (creepDied || heroDamage > 0) {
      this.hero.applyVampirism(heroDamage);
    }
    ```

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:**
  - [x] **Регенерация**: `this.health = Math.min(this.maxHealth, this.health + this.healthRegen*deltaTime/1000)`
  - [x] **Трата маны**: `this.energy--` при каждой атаке, пауза 2 сек если мана = 0  
  - [x] **Вампиризм**: `this.health = Math.min(this.health + this.vampirism, this.maxHealth)` после атаки
  - [x] **Отключение регенерации**: Во время атаки И когда poison активен
  - [x] **Скорости**: `healthRegen*deltaTime/1000` и `energyRegen*deltaTime/1000`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.4:**
**Что должно работать после выполнения микрошага 2.5.4:**
- [x] **Урон героя**: Клики должны наносить урон согласно `stats["damage"]` (изначально 10), крип должен терять HP постепенно
- [x] **Трата маны**: Каждая атака должна снимать 1 ману. При мане = 0 атаки должны блокироваться
- [x] **Регенерация HP**: HP героя должно восстанавливаться согласно `stats["health-regen"]` (изначально 1/сек)
- [x] **Регенерация маны**: Мана героя должна восстанавливаться согласно `stats["mana-regen"]` (изначально 0.5/сек)
- [x] **Вампиризм**: После атаки герой должен восстанавливать HP равное `stats["vampirism"]` (изначально 0)
- [x] **Отключение регенерации**: Регенерация НЕ работает во время атаки в коллизии
- [x] **Poison эффект**: Voul теперь ОТКЛЮЧАЕТ регенерацию HP на 1 секунду
- [x] **Синхронизация полосок**: Полоски должны отображать изменения HP/маны
- [x] **Консоль**: `heroStore.getState().stats` должно показывать изменения всех характеристик в реальном времени
- **✅ Если ВСЕ характеристики работают как в старом проекте - микрошаг 2.5.4 завершен!**

### ✅ Микрошаг 2.5.5 - Система смерти героя и награждения золотом
- [ ] **ПРОБЛЕМА**: Нет обработки смерти героя когда HP опускается до 0 И нет начисления золота за убийство крипов
- [ ] **РЕШЕНИЕ**: Реализовать комплексную систему Game Over с подсчетом золота как в старом проекте

- [ ] **Задача для AI**: Комплексное обновление GameController с системой смерти героя и золота

#### **ЧАСТЬ 1: Система награждения золотом** ✅
  - [x] **GameController.ts** - добавить систему золота:
    ```typescript
    // Новые поля для отслеживания золота
    private sessionGoldEarned: number = 0; // Золото за текущую сессию
    
    // Метод награждения золотом (вызывается в onCreepKilled)
    private awardGoldForKill(goldAmount: number, x?: number, y?: number): void {
      this.sessionGoldEarned += goldAmount;
      
      // Обновляем heroStore.stats["coins"]
      const currentStats = heroStore.getState().stats;
      const newCoins = currentStats["coins"] + goldAmount;
      
      // Синхронизируем с GoldContext и heroStore
      // Анимация монет при убийстве
    }
    
    // Геттеры
    public getSessionGoldEarned(): number { return this.sessionGoldEarned; }
    public resetSessionGold(): void { this.sessionGoldEarned = 0; }
    ```

  - [x] **Интеграция с убийством крипов**: В методе `onCreepKilled()` добавить:
    ```typescript
    // Получить goldReward из конфигурации крипа
    const creepConfig = getCreepConfig(this.currentCreep.getCreepType());
    if (creepConfig) {
      // Получаем позицию крипа для анимации монет
      const creepBounds = this.currentCreep.getBounds();
      const coinX = creepBounds.x + creepBounds.width / 2;
      const coinY = creepBounds.y + creepBounds.height / 2;
      
      this.awardGoldForKill(creepConfig.goldReward, coinX, coinY);
    }
    ```

  - [x] **CoinAnimation.ts** - создан компонент анимации монет:
    ```typescript
    // Система анимации "+X" золота при убийстве крипов
    // - Золотистый текст с черной обводкой
    // - Анимация подпрыгивания и исчезновения (1.5 сек)
    // - Иконка монеты рядом с текстом
    // - Полная интеграция с PixiJS
    ```

  - [x] **Интеграция с GoldContext** - мгновенная синхронизация UI:
    ```typescript
    // Глобальная функция updateGoldFromGameController
    // Мгновенное обновление Header при получении золота
    // Совместимость с пассивным доходом
    ```

#### **ЧАСТЬ 2: Обнаружение смерти героя**
  - [ ] **GameController.ts** - добавить проверку здоровья в основной цикл:
    ```typescript
    // В методе update() добавить проверку здоровья (как в старом проекте)
    if (this.hero && this.hero.getCurrentHealth() <= 0) {
      this.endGame(); // Завершаем игру, если здоровье равно 0
      return; // Останавливаем игровой цикл
    }
    ```

  - [ ] **GameController.ts** - добавить метод завершения игры:
    ```typescript
    // Новый метод завершения игры (аналог из старого проекта)
    private endGame(): void {
      this.isGameRunning = false; // Останавливаем игру
      // Останавливаем все звуки и анимации
      // Показываем модальное окно Game Over с золотом сессии
      // Синхронизируем золото с сервером
    }
    ```

#### **ЧАСТЬ 3: Game Over модальное окно**  
  - [ ] **Создать компонент**: `src/features/ui/GameOverModal/GameOverModal.tsx`
    ```typescript
    // Модальное окно Game Over с функциональностью:
    interface GameOverModalProps {
      isVisible: boolean;
      goldEarned: number; // Золото заработанное за текущую сессию
      totalGold: number;  // Общее золото героя
      onRestart: () => void;      // Начать заново (сбросить HP героя)
      onMainMenu: () => void;     // Вернуться в главное меню
    }
    ```

  - [ ] **Содержимое модального окна**:
    ```
    ☠️ ГЕРОЙ ПОГИБ ☠️
    
    Заработано золота за забег: +X
    Всего золота: Y
    
    [Начать заново]  [Главное меню]
    ```

#### **ЧАСТЬ 4: Интеграция с игровым циклом**
  - [ ] **GamePage.tsx** - интеграция Game Over модального окна:
    ```typescript
    // Добавить состояние Game Over и передать коллбэки в GameController
    const [isGameOver, setIsGameOver] = useState(false);
    const [goldEarned, setGoldEarned] = useState(0);
    
    // Коллбэки для модального окна
    const handleRestart = () => {
      // Восстановить HP героя до максимального
      // Сбросить счетчик заработанного золота сессии
      // Перезапустить игру
    };
    
    const handleMainMenu = () => {
      // Перейти на главную страницу /
    };
    ```

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:**
  - [ ] **Проверка смерти**: `if (hero.health <= 0) { endGame(); return; }` в игровом цикле (game.js:94)
  - [ ] **Завершение игры**: `isGameRunning = false; background.setSpeed(0); hero.stopSound(true);` (game.js:142-150)
  - [ ] **Начисление золота**: `hero.addCoins(Math.floor(this.coinsEarned**1.07**(this.creepRank)));` (creep.js:280)
  - [ ] **API синхронизация**: `updateGold(payload)` и `changeGold()` (creep.js:258, 281)
  - [ ] **Game Over экран**: Показ статистики и кнопки меню/рестарта (закомментированный код в game.js:146-152)

#### **⚠️ ВАЖНО: Анализ синхронизации золота в старом проекте:**
  - [ ] **В старом проекте было ТРИ типа синхронизации:**
    1. **При каждом убийстве крипа** - `updateGold({"userId": userId, "heroId": heroId, "income": 1})` (creep.js:259-260)
    2. **При переходе между страницами** - `await closeGame()` → `endGame()` → но без прямой синхронизации золота
    3. **При инициализации приложения** - `updateGold({'userId': userId, 'heroId': heroId})` без поля income (main.js:168)

  - [ ] **Современный подход для нашего проекта:**
    1. **Локальное накопление** - золото накапливается в `sessionGoldEarned` + обновляется в `heroStore.stats["coins"]`
    2. **Batch синхронизация при смерти** - отправляем весь accumulated gold одним запросом
    3. **Batch синхронизация при завершении волны** *(Фаза 3)* - когда волна пройдена успешно
  
  - [ ] **API endpoint**: `/update_user_money` с payload `{"userId": number, "heroId": number, "income": number}`
  - [ ] **Fallback**: Если синхронизация не удалась - сохранить в localStorage для повторной попытки

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.5:**
**Что должно работать после выполнения микрошага 2.5.5:**

**💰 Система золота:**
- [ ] **Золото за убийство**: После убийства крипа `stats["coins"]` должно увеличиваться на `goldReward` из конфигурации
- [ ] **Разные награды**: Разные типы крипов должны давать разное количество золота согласно `creepsConfig.ts`
- [ ] **Подсчет сессии**: Золото за текущую сессию должно отслеживаться отдельно
- [ ] **Синхронизация GoldContext**: Золото в GoldContext должно соответствовать `heroStore.stats["coins"]`
- [ ] **Синхронизация с MainPage**: Увеличение золота должно отображаться на `/`

**☠️ Система смерти:**
- [ ] **Смерть героя**: Когда `current-health` опускается до 0, игра должна останавливаться
- [ ] **Game Over модалка**: Должно появляться модальное окно с информацией о заработанном золоте ЗА СЕССИЮ
- [ ] **Кнопка "Начать заново"**: Должна восстанавливать HP героя и сбрасывать счетчик сессии
- [ ] **Кнопка "Главное меню"**: Должна возвращать на MainPage `/`
- [ ] **Остановка игры**: При смерти героя все звуки, анимации и игровой цикл должны останавливаться

**🔧 Техническая проверка:**
- [ ] **Консоль золота**: `heroStore.getState().stats["coins"]` должно увеличиваться после убийства крипа
- [ ] **Консоль смерти**: При смерти героя в консоли должно логироваться "Hero died" или аналогичное сообщение  
- [ ] **Локальное накопление**: `gameController.getSessionGoldEarned()` должно показывать золото текущей сессии
- [ ] **API синхронизация**: При смерти героя золото сессии должно отправляться на сервер одним запросом
- [ ] **Batch логика**: НЕ должно быть API запросов при каждом убийстве крипа (только локальное накопление)
- [ ] **Сохранение прогресса**: Заработанное золото должно сохраняться в heroStore и после перезапуска

- **✅ Если ВСЯ система золота И смерти героя работает - микрошаг 2.5.5 завершен!**

### ✅ Микрошаг 2.5.6 - Система паузы и схемы взаимодействия с пользователем
- [x] **ПРОБЛЕМА**: Нет системы паузы игры и четкой схемы взаимодействия при переходах между вкладками
- [x] **РЕШЕНИЕ**: Реализовать полную систему паузы и модальных окон для навигации

- [x] **Задача для AI**: Создать систему паузы и схему взаимодействия пользователя с игрой

#### **🎮 СХЕМА ВЗАИМОДЕЙСТВИЯ ПОЛЬЗОВАТЕЛЯ С ИГРОЙ:**

#### **СЦЕНАРИЙ 1: Смерть героя + "Начать заново"**
  - [ ] **Пользователь умирает** → Game Over модалка (уже реализовано в 2.5.5)
  - [ ] **Нажимает "Начать заново"**:
    1. ✅ **Золото сохраняется** - заработанное золото остается в `heroStore.stats["coins"]`
    2. ✅ **Герой восстанавливается** - HP/мана восстанавливаются до максимальных значений
    3. ✅ **Игра сбрасывается** - герой снова бежит, новый крип появляется за экраном
    4. ✅ **Счетчик сессии сбрасывается** - `sessionGoldEarned = 0` для новой сессии

#### **СЦЕНАРИЙ 2: Переход на другую вкладку во время игры**
  - [ ] **Пользователь кликает на другую вкладку** (главная/магазин) во время игры
  - [ ] **Игра автоматически ставится на паузу** 
  - [ ] **Появляется модальное окно подтверждения**:
    ```
    ⚠️ ПОКИНУТЬ ИГРУ?
    
    Вы уверены, что хотите покинуть игру?
    
    ✅ Заработанное золото за этот забег будет начислено
    ⚠️ Текущий прогресс будет потерян - придется начать заново
    
    [Да, покинуть]  [Остаться в игре]
    ```
  - [ ] **Если "Да, покинуть"**:
    1. ✅ **Золото сохраняется** - накопленное за сессию золото начисляется
    2. ✅ **Синхронизация с сервером** - отправляется batch запрос
    3. ✅ **Переход на выбранную вкладку**
    4. ✅ **Игра сбрасывается** - при возврате игра начнется заново
  - [ ] **Если "Остаться в игре"**:
    1. ✅ **Модалка закрывается**
    2. ✅ **Игра снимается с паузы** 
    3. ✅ **Продолжение игры** с того же места

#### **ЧАСТЬ 1: Система паузы игры** ✅
  - [x] **GameController.ts** - добавить методы паузы:
    ```typescript
    // Основные методы паузы
    public pauseGame(): void // ставит игру на паузу
    public resumeGame(): void // снимает игру с паузы
    public isPaused(): boolean // проверка состояния паузы
    
    // Управление игровым циклом
    private isGameRunning: boolean = true; // флаг работы игры
    private isPausedByUser: boolean = false; // флаг паузы пользователем
    
    // Обновленный метод update
    public update(deltaTime: number): void {
      if (!this.isGameRunning || this.isPausedByUser) {
        return; // Пропускаем обновление если игра на паузе
      }
      // ... существующий код update
    }
    ```

  - [x] **Интеграция с PixiJS Ticker** - правильная пауза анимаций:
    ```typescript
    // В GameController при паузе
    pauseGame(): void {
      this.isPausedByUser = true;
      // Остановить все анимации PixiJS
      if (this.hero) this.hero.pauseAnimations();
      if (this.currentCreep) this.currentCreep.pauseAnimations();
      // Остановить звуки
      // Показать overlay паузы
    }
    ```

#### **ЧАСТЬ 2: Модальное окно подтверждения навигации**
  - [ ] **Создать компонент**: `src/features/ui/NavigationConfirmModal/NavigationConfirmModal.tsx`
    ```typescript
    interface NavigationConfirmModalProps {
      isVisible: boolean;
      targetTab: string; // 'main' | 'shop'
      sessionGold: number; // Золото текущей сессии
      onConfirm: () => void; // Подтвердить переход
      onCancel: () => void;  // Остаться в игре
    }
    ```

#### **ЧАСТЬ 3: Интеграция с навигацией**
  - [ ] **App.tsx / GamePage.tsx** - перехват навигации:
    ```typescript
    // Обработчик попытки навигации
    const handleNavigationAttempt = (targetRoute: string) => {
      if (isGameActive && !isPaused) {
        // Поставить игру на паузу
        gameController.pauseGame();
        // Показать модальное окно подтверждения
        setNavigationModal({
          isVisible: true,
          targetTab: targetRoute,
          sessionGold: gameController.getSessionGoldEarned()
        });
      } else {
        // Обычная навигация если игра не активна
        navigate(targetRoute);
      }
    };
    ```

#### **ЧАСТЬ 4: Кнопка паузы (временно)** ✅
  - [x] **Добавить кнопку паузы в верхнее меню** (как в старом проекте):
    ```typescript
    // Интеграция с кнопкой настроек в Header.tsx
    // При клике на настройки - игра ставится на паузу
    // При закрытии настроек - игра снимается с паузы
    ```
  - [x] **РЕАЛИЗОВАНО**: 
    - Создан `GameContext` для централизованного управления паузой
    - Кнопка настроек в Header теперь управляет паузой игры  
    - При открытии настроек - игра автоматически на паузе
    - При закрытии настроек - игра автоматически возобновляется
    - Иконка остается шестеренкой (настройки), готова к добавлению модального окна
    - Исправлен баг с кликами во время паузы - теперь герой не атакует на паузе

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:**
  - [ ] **Пауза игры**: `isGameRunning = false` останавливает `requestAnimationFrame` (game.js:82-96)
  - [ ] **Управление звуками**: `hero.stopSound()` и `hero.resumeSound()` (game.js:115, 125)
  - [ ] **Кнопки паузы**: `pause-button` и `resume-button` с обработчиками (game.js:183-220)
  - [ ] **Переходы между вкладками**: `closeGame()` вызывается перед навигацией (main.js:100)

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.6:**
**Что должно работать после выполнения микрошага 2.5.6:**

**⏸️ Система паузы:**
- [x] **Кнопка паузы**: В верхнем меню кнопка настроек ⚙️ управляет паузой игры
- [x] **Пауза работает**: При открытии настроек игра останавливается (герой, крип, анимации, фон)
- [x] **Снятие паузы**: При закрытии настроек игра возобновляется с того же места
- [x] **Блокировка кликов**: Во время паузы клики по экрану не вызывают атаки героя
- [x] **GameContext**: Централизованное управление паузой через React Context

**🔄 Навигация во время игры:** ⏳ (СЛЕДУЮЩИЙ ЭТАП)
- [ ] **ЧАСТЬ 2: Модальное окно подтверждения навигации** - НЕ РЕАЛИЗОВАНО
- [ ] **ЧАСТЬ 3: Интеграция с навигацией** - НЕ РЕАЛИЗОВАНО  
- [ ] **Перехват навигации**: При клике на другую вкладку во время игры должна появляться модалка
- [ ] **Модалка подтверждения**: Должно появляться окно с предупреждением о потере прогресса
- [ ] **"Да, покинуть"**: Золото должно начисляться, игра сбрасываться, переход на выбранную вкладку
- [ ] **"Остаться"**: Модалка закрывается, игра продолжается
- [ ] **Автопауза**: Игра автоматически ставится на паузу при появлении модалки

**🎮 Сценарии взаимодействия:**
- [ ] **Смерть + перезапуск**: Золото сохраняется, герой восстанавливается, игра начинается заново
- [ ] **Навигация + возврат**: При возврате в игру после навигации игра должна начинаться заново
- [ ] **Сохранение прогресса**: Заработанное золото должно сохраняться во всех сценариях

**🔧 Техническая проверка:**
- [ ] **Консоль паузы**: При паузе в консоли должно логироваться "Game paused"
- [ ] **Консоль навигации**: При попытке навигации должно логироваться предупреждение
- [ ] **Состояние игры**: `gameController.isPaused()` должно корректно отражать состояние
- [ ] **Синхронизация**: При принудительном выходе золото должно синхронизироваться с сервером

- **🟡 ЧАСТИЧНО ЗАВЕРШЕН - Система паузы работает, навигационные модалки - следующий этап**

#### **✅ ЧТО ЗАВЕРШЕНО В МИКРОШАГЕ 2.5.6:**
- ✅ **Полная система паузы игры** - готова к работе
- ✅ **Интеграция с кнопкой настроек** - элегантное решение вместо отдельной кнопки
- ✅ **GameContext** - централизованное управление состоянием игры
- ✅ **Блокировка кликов на паузе** - исправлен важный баг
- ✅ **Готовность к модальным окнам** - инфраструктура создана

#### **⏳ ЧТО ОСТАЛОСЬ НА СЛЕДУЮЩИЙ ЭТАП:**
- ⏳ Модальное окно подтверждения навигации
- ⏳ Перехват попыток навигации 
- ⏳ Интеграция с роутингом приложения

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 2.5:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 2.5:**

**🎮 Тестирование игры `/game`:**
- [x] **Характеристики героя**: Открыть консоль браузера → `heroStore.getState().stats` должно показать все поля включая `level`, `coins`, `current-health`, `current-mana` ✅
- [x] **Крипы с HP**: Крипы должны иметь полоски здоровья и НЕ умирать от одного клика ✅
- [ ] **Урон героя**: Клики должны наносить урон согласно `stats["damage"]` (изначально 10), крип должен терять HP постепенно 
- [x] **Атаки крипов**: При коллизии крип должен атаковать героя и уменьшать его `current-health`
- [ ] **Трата маны**: Каждая атака должна снимать 1 ману. При мане = 0 атаки должны блокироваться
- [ ] **Регенерация HP**: HP героя должно восстанавливаться со временем согласно `health-regen` (1/сек)
- [ ] **Регенерация маны**: Мана героя должна восстанавливаться со временем согласно `mana-regen` (0.5/сек)
- [ ] **Вампиризм**: После атаки герой должен восстанавливать HP равное `vampirism` (изначально 0)
- [ ] **Золото за убийство**: За убийство крипа должно добавляться золото в `stats["coins"]`
- [ ] **Особые способности**: 
  - Satyr должен отнимать ману при атаке (manaburn)
  - Voul должен отключать регенерацию HP на 1 секунду (poison)

**📊 Тестирование MainPage `/`:**
- [ ] Должны отображаться ВСЕ характеристики включая `level`, `coins`, `current-health`, `current-mana`
- [ ] Значения должны соответствовать тем что в игре (синхронизация)
- [ ] После игры и получения золота - на MainPage должно отображаться обновленное количество `coins`

**🔧 Техническая проверка:**
- [x] Консоль браузера: НЕТ ошибок связанных с heroStore или характеристиками ✅
- [x] `heroStore.getState().stats` содержит все 12 полей ✅
- [ ] `GoldContext` и `heroStore.stats["coins"]` синхронизированы
- [x] Разные типы крипов имеют разные HP, урон и награды ✅
- [x] Полоски здоровья работают для крипов и героя ✅
- [x] Крипы имеют реальное здоровье и получают урон ✅

**✅ Если все пункты выполнены - Фаза 2.5 завершена успешно!**

---

## 📋 Фаза 3: Система волн

### ✅ Микрошаг 3.1 - Счетчик убитых крипов
- [ ] **Задача для AI**: Добавить счетчик в GameController
  - [ ] Переменная для подсчета убитых крипов в волне
  - [ ] Логика для отслеживания прогресса волны
  - [ ] Условие завершения волны (например, 10 крипов)

### ✅ Микрошаг 3.2 - Система боссов
- [ ] **Задача для AI**: Добавить боссов в конце волны
  - [ ] После убийства всех обычных крипов спавнить босса
  - [ ] Босс = обычный крип но с увеличенным масштабом (x2) и здоровьем
  - [ ] Сделать босса из любого доступного типа крипа

### ✅ Микрошаг 3.3 - Простой HUD волны
- [ ] **Задача для AI**: Создать `src/features/ui/WaveHUD/WaveHUD.tsx`
  - [ ] Показать текущий прогресс волны: "Убито: 3/10"
  - [ ] Индикатор когда идет босс: "БОСС!"
  - [ ] Простой стиль, в углу экрана

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 3:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 3:**
- [ ] Запустить игру `/game`
- [ ] В углу экрана должен быть HUD с текстом "Убито: 0/10"
- [ ] После убийства каждого крипа счетчик должен увеличиваться: "Убито: 1/10", "Убито: 2/10" и т.д.
- [ ] После убийства 10-го крипа должен появиться БОСС
- [ ] HUD должен показать "БОСС!" вместо счетчика
- [ ] Босс должен быть в 2 раза больше обычного крипа и с большим здоровьем
- [ ] После убийства босса счетчик должен сброситься и начаться новая волна
- **✅ Если система волн работает с HUD - Фаза 3 завершена!**

---

## 📋 Фаза 4: Простая система уровней героя

### ✅ Микрошаг 4.1 - HeroLevelSystem без опыта
- [ ] **Задача для AI**: Создать `src/game/systems/HeroLevelSystem.ts` (папка systems пустая)
  - [ ] Класс `HeroLevelSystem` с методами:
    - `getCurrentLevel(): number`
    - `levelUp(): void`
    - `getMaxLevel(): number` (30)
    - `canLevelUp(): boolean`
  - [ ] События через EventEmitter:
    - `onLevelUp(newLevel: number)`
  - [ ] Простое сохранение в localStorage

### ✅ Микрошаг 4.2 - Модалка завершения волны
- [ ] **Задача для AI**: Создать `src/features/ui/WaveCompleteModal/WaveCompleteModal.tsx`
  - [ ] Модальное окно после убийства босса:
    - "Волна пройдена!"
    - "Уровень героя повышен до X!"
    - Кнопка "Следующая волна"
  - [ ] Простые анимации появления

### ✅ Микрошаг 4.3 - UI уровня героя
- [ ] **Задача для AI**: Создать `src/features/ui/HeroLevelDisplay/HeroLevelDisplay.tsx`
  - [ ] Простой компонент в углу экрана:
    - "Уровень героя: X"
    - Максимум 30 уровней
  - [ ] Интеграция с HeroLevelSystem

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 4:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 4:**
- [ ] Запустить игру `/game`
- [ ] В углу экрана должно отображаться "Уровень героя: 1"
- [ ] Пройти одну волну (убить 10 крипов + босса)
- [ ] После убийства босса должна появиться модалка "Волна пройдена! Уровень героя повышен до 2!"
- [ ] После закрытия модалки уровень героя должен отображаться как "Уровень героя: 2"
- [ ] Каждая пройденная волна должна повышать уровень на 1
- [ ] Проверить что система сохраняется в localStorage (обновить страницу - уровень должен сохраниться)
- **✅ Если система уровней работает с модалкой и UI - Фаза 4 завершена!**

---

## 📋 Фаза 5: Продвинутые уровни

### ✅ Микрошаг 5.1 - Конфигурация уровней в коде
- [ ] **Задача для AI**: Создать `src/game/config/levelsConfig.ts` (новый файл)
  - [ ] Простой массив конфигураций для уровней 1-30:
    ```typescript
    interface LevelConfig {
      level: number;
      creepTypes: string[]; // какие крипы на уровне
      creepCount: number;   // сколько крипов (10-15)
      bossType: string;     // тип босса
    }
    ```
  - [ ] Функции для получения конфигурации уровня
  - [ ] Прогрессия сложности от уровня к уровню

### ✅ Микрошаг 5.2 - Система перехода между уровнями
- [ ] **Задача для AI**: Интеграция уровней с волнами
  - [ ] При завершении волны проверить максимальный уровень
  - [ ] Загружать конфигурацию следующего уровня
  - [ ] Спавнить крипов согласно конфигурации уровня

### ✅ Микрошаг 5.3 - Финальная полировка
- [ ] **Задача для AI**: Доработки и исправления
  - [ ] Балансировка сложности уровней
  - [ ] Проверка всех анимаций крипов
  - [ ] Оптимизация производительности
  - [ ] Тестирование полного цикла игры (1-30 уровни)

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 5 (ФИНАЛЬНАЯ):**
**Что должно быть видно на экране после выполнения всех шагов Фазы 5:**
- [ ] Запустить игру `/game` 
- [ ] Проверить что уровни 1-5 имеют разную сложность (разные типы крипов)
- [ ] На уровне 1 должны быть простые крипы (например, только direCreep и wolf)
- [ ] На уровне 5 должны быть более сложные крипы (например, medved и voul)
- [ ] Количество крипов в волне может варьироваться от 10 до 15 в зависимости от уровня
- [ ] Боссы должны быть разных типов в зависимости от уровня
- [ ] Система должна работать стабильно и плавно
- [ ] Пройти несколько уровней подряд - все должно работать без ошибок
- **✅ Если продвинутая система уровней работает - ВСЯ СИСТЕМА ЗАВЕРШЕНА!**

---

## 🎯 **ДОПОЛНИТЕЛЬНЫЕ КОНТРОЛЬНЫЕ ТОЧКИ:**

### 🕐 **Быстрая проверка после каждого микрошага:**
- **После 1.1**: В консоли браузера не должно быть ошибок загрузки новых спрайтов
- **После 1.2**: Крип должен корректно создаваться с новым типом
- **После 2.1**: При перезагрузке должны появляться разные крипы
- **После 3.1**: В консоли должны логироваться убийства крипов со счетчиком
- **После 3.2**: Должен появляться увеличенный босс
- **После 4.1**: В localStorage должен сохраняться уровень героя
- **После 4.2**: Должна появляться модалка после убийства босса

### ⚠️ **Если что-то не работает:**
1. Проверить консоль браузера на ошибки
2. Убедиться что все файлы спрайтов находятся в папке `/public/media/game/assets/creeps/`
3. Проверить что пути к файлам в коде совпадают с реальными путями
4. Перезагрузить страницу и очистить кэш браузера

---

## 🎯 Приоритеты выполнения

### 🥇 Поэтапная реализация (делаем по порядку)
1. **Фаза 1** - Добавление новых крипов (1.1 → 1.2 → 1.3)
2. **Фаза 2** - Рандомизация крипов (2.1 → 2.2 → 2.3)  
3. **Фаза 3** - Система волн (3.1 → 3.2 → 3.3)
4. **Фаза 4** - Простая система уровней (4.1 → 4.2 → 4.3)
5. **Фаза 5** - Продвинутые уровни (5.1 → 5.2 → 5.3)

### 🎯 Философия: наглядность на каждом шаге
- После каждого микрошага должен быть видимый результат
- Тестируем каждую фичу сразу после реализации
- Не переходим к следующему шагу пока не убедились что текущий работает

---

## 📝 Технические детали крипов

### Спрайтшиты крипов (путь: `public/media/game/assets/creeps/`)

**medved:**
- `medved_attack.png` - 5x5 grid, 21 кадр
- `medved_death.png` - 5x5 grid, 21 кадр  
- `medved_idle.png` - 5x6 grid, 27 кадров

**satyr:**
- `satyr_attack.png` - 5x5 grid, 23 кадра
- `satyr_death.png` - 5x6 grid, 30 кадров
- `satyr_idle.png` - 6x7 grid, 40 кадров

**shishka:**
- `shishka_attack.png` - 5x6 grid, 27 кадров
- `shishka_death.png` - 5x6 grid, 27 кадров
- `shishka_idle.png` - 5x5 grid, 24 кадра

**voul:**
- `voul_attack.png` - 5x5 grid, 25 кадров
- `voul_death.png` - 4x8 grid, 31 кадр
- `voul_idle.png` - 7x5 grid, 34 кадра

**wolf:**
- `wolf_attack.png` - 3x7 grid, 21 кадр
- `wolf_death.png` - 3x6 grid, 18 кадров
- `wolf_idle.png` - 5x5 grid, 23 кадра

### Принципы работы
- Один уровень = одна волна крипов + босс в конце
- Прошел волну = повысил уровень героя
- Не прошел = остался на том же уровне
- Максимум 30 уровней героя
- Все настройки крипов и уровней - в коде (не визуальная админка)

---

## 🚀 Готов начать?

**Следующий шаг**: Начать с Микрошага 1.1 - расширение AssetsManager для новых крипов

### 🔥 Почему именно такая последовательность?
1. **Сначала крипы** - добавим всех, увидим как они выглядят
2. **Потом рандом** - сделаем игру разнообразнее 
3. **Затем волны** - добавим счетчик и боссов
4. **После уровни** - простая система прогресса
5. **В конце сложность** - настройка уровней по сложности

Каждый шаг даст видимый результат! 🎯 