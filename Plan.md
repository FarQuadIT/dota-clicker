# 🎮 План разработки системы уровней - Dota Clicker

## 🎯 Общая цель
Создать простую систему уровней героя (1-30) где один уровень = одна волна крипов + босс. Прошел уровень - повысил уровень героя, не прошел - остался на том же.

---

## 📋 Фаза 1: Добавление новых типов крипов

### ✅ Микрошаг 1.1 - Расширение AssetsManager для новых крипов
- [x] **Задача для AI**: Обновить `src/game/managers/AssetsManager.ts`
  - [x] Добавить в манифест (метод `createAssetManifest`) новых крипов:
    - **medved**: attack(5x5, 21 кадр), death(5x5, 21 кадр), idle(5x6, 27 кадров)
    - **satyr**: attack(5x5, 23 кадра), death(5x6, 30 кадров), idle(6x7, 40 кадров)  
    - **shishka**: attack(5x6, 27 кадров), death(5x6, 27 кадров), idle(5x5, 24 кадра)
    - **voul**: attack(5x5, 25 кадров), death(4x8, 31 кадр), idle(7x5, 34 кадра)
    - **wolf**: attack(3x7, 21 кадр), death(3x6, 18 кадров), idle(5x5, 23 кадра)
  - [x] Путь к файлам: `/media/game/assets/creeps/`
  - [x] frameWidth и frameHeight нужно подобрать по аналогии с direCreep

### ✅ Микрошаг 1.2 - Обновление Creep класса
- [x] **Задача для AI**: Обновить `src/game/entities/Creep.ts`
  - [x] Добавить параметр `creepType: string` в конструктор
  - [x] Изменить вызов `assetsManager.getCreepFrames('direCreep', ...)` на использование переданного типа
  - [x] Поддержка всех 6 типов: direCreep, medved, satyr, shishka, voul, wolf
  - [x] Сохранить адаптивное позиционирование

### ✅ Микрошаг 1.3 - Тестирование одного нового крипа
- [x] **Задача для AI**: Обновить GameController для тестирования
  - [x] В методе `createNewCreep()` передать другой тип крипа в конструктор (например 'medved')
  - [x] Проверить что анимации работают корректно
  - [x] Убедиться что позиционирование адаптивное

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 1:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 1:**
- [x] Запустить игру `/game`
- [x] Вместо стандартного direCreep должен появиться новый крип (wolf)
- [x] Новый крип должен иметь анимации idle, attack, death
- [x] Крип корректно позиционируется и движется к герою
- [x] При клике герой атакует нового крипа и он умирает
- **✅ Если все работает - Фаза 1 завершена успешно!**

---

## 📋 Фаза 2: Рандомизация крипов

### ✅ Микрошаг 2.1 - Простая рандомизация
- [x] **Задача для AI**: Обновить GameController для рандомного выбора крипов
  - [x] Добавить массив доступных типов крипов в класс GameController
  - [x] В методе `createNewCreep()` случайно выбирать тип из массива
  - [x] Проверить что все типы крипов спавнятся корректно

### ✅ Микрошаг 2.2 - Конфигурация типов крипов
- [x] **Задача для AI**: Создать `src/game/config/creepsConfig.ts` (файл уже есть, но пустой)
  - [x] Добавить конфигурацию `CREEP_TYPES` с параметрами каждого типа:
    ```typescript
    export interface CreepTypeConfig {
      scale: number;
      health: number;
      speed: number;
    }
    
    export const CREEP_TYPES: Record<string, CreepTypeConfig> = {
      direCreep: { scale: 1.0, health: 100, speed: 2 },
      medved: { scale: 1.2, health: 150, speed: 1.5 },
      satyr: { scale: 0.9, health: 80, speed: 2.5 },
      // и т.д. для всех типов
    }
    ```
  - [x] Использование конфигурации при создании крипов в GameController

### ✅ Микрошаг 2.3 - Тестирование всех крипов
- [x] **Задача для AI**: Убедиться что работают все крипы
  - [x] Протестировать каждый тип крипа по отдельности
  - [x] Проверить анимации attack, death, idle для всех
  - [x] Убедиться в корректном масштабировании

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 2:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 2:**
- [x] Запустить игру `/game`
- [x] Перезагружать страницу несколько раз - каждый раз должен появляться случайный крип
- [x] Должны появляться все 6 типов: direCreep, medved, satyr, shishka, voul, wolf
- [x] Каждый тип крипа должен иметь свой размер согласно конфигурации
- [x] Все крипы корректно анимируются и реагируют на атаки героя
- [x] **ДОПОЛНИТЕЛЬНО**: Выполнить `testAllCreeps()` в консоли для проверки конфигурации
- **✅ Если рандомизация работает и все крипы отображаются - Фаза 2 завершена!**

---

## 📋 Фаза 2.5: Интеграция характеристик из старого проекта

### ✅ Микрошаг 2.5.1 - Настройка героя и загрузка характеристик
  - [x] **Задача для AI**: Расширение HeroStats и интеграция с реальными данными
    - [x] **ПРОБЛЕМА**: В `HeroStats` нет полей `level` и `coins`, но они есть в `RawHeroData` с сервера
    - [x] **РЕШЕНИЕ**: Добавить в `HeroStats` недостающие поля:
    ```typescript
    /** Текущий уровень героя */
    "level": number;
    
    /** Текущее количество золота */
    "coins": number;
    
    /** Текущее здоровье героя (не максимальное) */
    "current-health": number;
    
    /** Текущая мана героя (не максимальная) */
    "current-mana": number;
    ```
  - [x] **Обновление mappers.ts**: Добавить маппинг новых полей в `mapHeroData()`:
    ```typescript
    "level": data.level ?? 1,
    "coins": data.coins ?? 0,
    "current-health": data.maxHealth ?? 100, // Изначально = максимальному
    "current-mana": data.maxEnergy ?? 50,    // Изначально = максимальной
    ```
  - [x] **Интеграция с GameController**:
    - Добавить в `GameController` метод `getHeroStats()` для получения характеристик из heroStore
    - Урон героя должен браться из `stats["damage"]` вместо константы
    - HP и мана героя должны отслеживаться в реальном времени
  - [x] **Убрать инициализацию базовыми данными** - теперь данные берутся с сервера через App.tsx

### ✅ Микрошаг 2.5.1+ - Визуальные полоски HP/MP над героем
  - [x] **Задача для AI**: Создать визуальные полоски здоровья и маны над джаггернаутом
    - [x] **Изучить старый проект**: В `old_project_game/front1/game/heroes/hero.js` метод `drawBars()` показывает как рисовались полоски
    - [x] **Современное решение**: Использовать PixiJS Graphics вместо Canvas 2D
    - [x] **Создать компонент**: `src/game/components/HeroHealthBar.ts` с классом для отображения полосок
    - [x] **Интеграция с Hero.ts**: Добавить полоски как дочерние элементы героя
    - [x] **Особенности из старого проекта**:
    ```typescript
    // Полоска здоровья (красная)
    - Фон: rgba(255, 0, 0, 0.8) или rgba(66, 102, 0, 0.8) при отсутствии регена
    - Заполнение: #ff4d4d или #88cc00
    - Текст: "current/max" по центру белым цветом
    
    // Полоска маны (синяя) 
    - Фон: rgba(0, 0, 255, 0.8) или rgba(0, 0, 41, 0.8) при отсутствии регена
    - Заполнение: #00bfff
    - Текст: "current/max" по центру белым цветом
    - Позиция: под полоской здоровья с отступом 5px
    ```
  - [x] **Плавная анимация**: Как в старом проекте - плавное изменение полосок при изменении HP/MP
  - [x] **Адаптивность**: Полоски должны масштабироваться с героем и быть всегда над ним
  - [x] **Синхронизация с heroStore**: Полоски должны отображать значения из `stats["current-health"]`, `stats["current-mana"]`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.1:**
**Что должно работать после выполнения микрошага 2.5.1:**
- [x] **Консоль браузера**: `heroStore.getState().stats` должно показать ВСЕ 12 полей
- [x] **MainPage `/`**: Должны отображаться новые поля `level`, `coins`, `current-health`, `current-mana`
- [x] **Инициализация**: Все характеристики должны иметь базовые значения из старого проекта
- [x] **Интеграция**: GameController должен получать урон героя из `heroStore.stats["damage"]` (пока еще не используется, но метод есть)
- [x] **НЕТ ошибок** в консоли браузера связанных с типами или отсутствующими полями

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.1+:**
**Что должно быть ВИЗУАЛЬНО видно после выполнения микрошага 2.5.1+:**
- [x] **Игровая страница `/game`**: Над джаггернаутом должны отображаться 2 полоски:
  - **Красная полоска HP** с текстом "100/100" (или текущие значения)
  - **Синяя полоска MP** снизу с текстом "50/50" (или текущие значения)
- [x] **Адаптивность**: При изменении размера окна полоски должны оставаться над героем
- [x] **Плавная анимация**: Полоски должны плавно изменяться при изменении HP/MP
- [x] **Синхронизация**: Значения в полосках должны соответствовать `heroStore.stats["current-health"]` и `stats["current-mana"]`
- [x] **Стиль как в старом проекте**: Цвета и размеры должны быть похожи на оригинал
- [x] **Исправлен баг с заполнением**: Полоски сразу отображаются полными без анимации заполнения
- [x] **Исправлен баг с характеристиками**: При входе в игру все характеристики обновляются с сервера
- **✅ Микрошаг 2.5.1+ ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.2 - Реализация характеристик и особых умений крипов  
- [ ] **Задача для AI**: Расширение `src/game/config/creepsConfig.ts`
  - [ ] **ПРОБЛЕМА**: В `CreepTypeConfig` только визуальные параметры, нет боевых характеристик
  - [ ] **РЕШЕНИЕ**: Добавить реальные боевые характеристики:
    ```typescript
    interface CreepTypeConfig {
      // ... существующие поля ...
      
      /** Максимальное здоровье крипа */
      maxHealth: number;
      
      /** Урон который наносит крип герою */
      damage: number;
      
      /** Сколько золота дает за убийство */
      goldReward: number;
      
      /** На каком уровне героя открывается крип */
      requiredHeroLevel: number;
      
      /** Особые способности крипа */
      specialAbilities?: string[];
    }
    ```
  - [ ] **Данные из старого проекта** `old_project_game/front1/game/creepData/creepsConfig.js`:
    ```
    direCreep: maxHealth=10, damage=3, goldReward=1, requiredHeroLevel=0
    shishka: maxHealth=15, damage=2, goldReward=1, requiredHeroLevel=1  
    medved: maxHealth=10, damage=1, goldReward=1, requiredHeroLevel=2
    wolf: maxHealth=5, damage=5, goldReward=1, requiredHeroLevel=3
    satyr: maxHealth=7, damage=2, goldReward=1, requiredHeroLevel=4, abilities=["manaburn"]
    voul: maxHealth=10, damage=2, goldReward=1, requiredHeroLevel=5, abilities=["poison"]
    ```
     - [ ] **Особые способности**:
     - `"manaburn"` (satyr): отнимает ману у героя равную урону
     - `"poison"` (voul): отключает регенерацию HP героя на 1 секунду

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.2:**
**Что должно работать после выполнения микрошага 2.5.2:**
- [x] **Консоль браузера**: `getCreepConfig('direCreep')` должно показать все новые поля (maxHealth, damage, goldReward, requiredHeroLevel)
- [x] **Конфигурация крипов**: Выполнить `testCreepConfigurations()` - все крипы должны спавниться без ошибок
- [x] **Данные из старого проекта**: Проверить что характеристики соответствуют:
  - `direCreep`: maxHealth=10, damage=3, goldReward=1, requiredHeroLevel=0
  - `wolf`: maxHealth=5, damage=5, goldReward=1, requiredHeroLevel=3
  - `satyr`: specialAbilities=["manaburn"]
  - `voul`: specialAbilities=["poison"]
- [x] **НЕТ ошибок** в консоли при загрузке конфигурации крипов
- [x] **Игра работает**: `/game` должна работать как раньше (крипы пока умирают от 1 хита, но конфигурация загружается)
- [x] **Функции тестирования**: Добавлены глобальные функции для консоли браузера
- **✅ Микрошаг 2.5.2 ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.2+ - Визуальные полоски HP крипов
- [x] **Задача для AI**: Создать полоски здоровья для крипов по аналогии с старым проектом
  - [x] **Изучить старый проект**: В `old_project_game/front1/game/creepData/creep.js` метод `drawHealthBar()` показывает как рисовались полоски крипов
  - [x] **Современное решение**: Использовать PixiJS Graphics вместо Canvas 2D
  - [x] **Создать компонент**: `src/game/components/CreepHealthBar.ts` с классом для отображения полоски крипа
  - [x] **Интеграция с Creep.ts**: Добавить полоску как элемент сцены, позиционирующийся относительно крипа
  - [x] **Особенности из старого проекта**:
    ```typescript
    // Полоска здоровья крипа (красная)
    - Размер: barWidth = creepWidth * scale / 3, barHeight = 15
    - Фон: rgba(255, 0, 0, 0.8) (полупрозрачный красный)
    - Заполнение: #ff4d4d (ярко-красный)
    - Текст: "currentHP/maxHP" по центру белым цветом (10px Arial)
    - Позиция: над крипом с настройкой через xBarDelta и yBarDelta из конфигурации
    ```
  - [x] **Адаптивное позиционирование**: 
    ```typescript
    // Координаты из старого проекта:
    const barX = creepX + (creepWidth * scale - barWidth) / 2 + imgConfigs.xBarDelta;
    const barY = creepY + imgConfigs.yBarDelta * creepY;
    ```
  - [x] **Интеграция с конфигурацией**: Использовать параметры `xBarDelta` и `yBarDelta` для каждого типа крипа
  - [x] **Скрытие при смерти**: Полоска НЕ отображается если крип умирает (isDying = true)
  - [x] **Обновление в реальном времени**: Полоска отображает актуальное здоровье крипа и обновляется при нанесении урона
  - [x] **Интеграция с боевой системой**: Крипы теперь имеют реальное здоровье и получают урон через `takeDamage()`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.2+:**
**Что должно быть ВИЗУАЛЬНО видно после выполнения микрошага 2.5.2+:**
- [x] **Игровая страница `/game`**: Над каждым крипом должна отображаться красная полоска здоровья:
  - **Красная полоска HP** с текстом "10/10" (или текущие значения согласно конфигурации)
  - **Правильное позиционирование**: полоска должна быть ровно над крипом
  - **Адаптивность**: при изменении размера окна полоска должна оставаться над крипом
- [x] **Разные типы крипов**: Полоски должны отображаться у всех типов крипов (direCreep, wolf, satyr, etc.)
- [x] **Скрытие при смерти**: Когда крип умирает, полоска должна исчезать
- [x] **Стиль как в старом проекте**: Размеры, цвета и шрифт должны быть похожи на оригинал
- [x] **Синхронизация**: Значения в полоске должны соответствовать реальному здоровью крипа
- [x] **Настройка позиций**: Для разных типов крипов полоски должны позиционироваться по-разному (используя xBarDelta/yBarDelta)
- [x] **Реальная боевая система**: Крипы не умирают от одного удара, имеют здоровье согласно конфигурации
- **✅ Микрошаг 2.5.2+ ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.3 - Реализация атак крипов на героя
- [x] **ПРОБЛЕМА**: Крипы НЕ атакуют героя. В старом проекте крипы наносили урон через `dealDamage(hero)` в определенных кадрах анимации атаки
- [x] **РЕШЕНИЕ**: Реализовать только атаки крипов без регенерации (регенерация будет в следующем шаге)

- [x] **Задача для AI**: Обновление `Hero.ts` и `Creep.ts` для атак крипов
  - [x] **Hero.ts** - добавить методы получения урона:
    ```typescript
    // Новые методы для получения урона (БЕЗ регенерации пока)
    takeDamage(damage: number): void // уменьшает current-health в heroStore
    takeManaburn(damage: number): void // уменьшает current-mana (способность сатира)
    applyPoison(duration: number): void // устанавливает флаг отравления (способность вула)
    
    // Геттеры для состояния
    isAlive(): boolean // проверка что HP > 0
    isPoisoned(): boolean // проверка активен ли яд
    ```

  - [x] **Creep.ts** - добавить атаки героя:
    ```typescript
    // Новые методы для атак героя (аналог старого проекта)
    attackHero(hero: Hero): void // наносит урон герою + особые способности
    canAttackHero(): boolean // проверка может ли атаковать
    
    // Интеграция с анимациями (как в старом проекте)
    private checkAttackFrame(): void // проверяет кадр атаки для нанесления урона
    ```

  - [x] **Изучить старый проект**: Метод `dealDamage(hero)` в `old_project_game/front1/game/creepData/creep.js`:
    ```javascript
    dealDamage(hero) {
      if (hero.health > 0) {
        hero.health = Math.max(0, hero.health - this.creepDamage);
      }
      // Особые способности через functions['attack_modifier']
      if (this.functions['attack_modifier']) {
        this.functions['attack_modifier'](hero);
      }
    }
    ```

  - [x] **Фокус**: Только атаки крипов, регенерация будет в следующем микрошаге

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.3:**
**Что должно работать после выполнения микрошага 2.5.3:**
- [x] **Атаки крипов**: При коллизии крип должен атаковать героя и уменьшать его `current-health` в heroStore
- [x] **Урон от крипов**: Разные типы крипов должны наносить разный урон согласно `creepsConfig.ts`
- [x] **Особые способности крипов**:
  - Satyr: при атаке должен уменьшать `stats["current-mana"]` (manaburn)
  - Voul: должен устанавливать флаг отравления (poison) - но регенерация пока НЕ отключается
- [x] **Синхронизация полосок**: Полоски HP/маны героя должны отображать актуальные значения из heroStore
- [x] **Синхронизация с MainPage**: Изменения HP/маны должны отображаться на `/`
- [x] **Консоль**: `heroStore.getState().stats["current-health"]` должно уменьшаться при получении урона
- [x] **НЕТ регенерации**: HP и мана НЕ восстанавливаются (это будет в следующем микрошаге)
- **✅ Микрошаг 2.5.3 ЗАВЕРШЕН И ПРОВЕРЕН!**

### ✅ Микрошаг 2.5.4 - Полная реализация характеристик героя
- [x] **ПРОБЛЕМА**: Многие характеристики героя не работают. Реализованы только полоски HP/маны и получение урона. НЕ работают: регенерация, урон героя, трата маны на атаки, вампиризм
- [x] **РЕШЕНИЕ**: Реализовать ВСЕ базовые характеристики геймплея как в старом проекте

- [x] **Задача для AI**: Комплексное обновление `Hero.ts`, `GameController.ts` и `Creep.ts`

#### **ЧАСТЬ 1: Урон героя по крипам**
  - [x] **ПРОБЛЕМА**: Урон героя НЕ используется в игре. В `GameController.getHeroDamage()` метод уже есть, но крипы получают фиксированный урон
  - [x] **GameController.ts** - исправить метод `dealDamageToCreep()`:
    ```typescript
    // ИСПРАВИТЬ: Должен использовать урон из heroStore.stats["damage"]
    const heroDamage = this.getHeroDamage(); // уже есть!
    const creepDied = this.currentCreep.takeDamage(heroDamage); // уже есть!
    // + добавить трату маны на атаку (см. следующую часть)
    ```

#### **ЧАСТЬ 2: Система маны и атак**
  - [x] **ПРОБЛЕМА**: В старом проекте каждая атака снимала 1 ману. Если мана = 0, была пауза на 2 секунды
  - [x] **GameController.ts** - добавить проверку маны в `dealDamageToCreep()`:
    ```typescript
    // ДОБАВИТЬ проверку маны перед атакой
    public dealDamageToCreep(): void {
      const currentMana = this.hero.getCurrentMana();
      if (currentMana <= 0) {
        // Нет маны - нельзя атаковать
        return;
      }
      
      // Тратим 1 ману на атаку
      this.hero.spendMana(1);
      
      // ... существующий код атаки
    }
    ```

  - [x] **Hero.ts** - добавить методы управления маной:
    ```typescript
    // Новые методы для работы с маной
    spendMana(amount: number): void // тратит ману на атаку
    canAttack(): boolean // проверка есть ли мана для атаки
    ```

#### **ЧАСТЬ 3: Система регенерации**
  - [x] **Hero.ts** - добавить систему регенерации:
    ```typescript
    // Система регенерации (аналог из старого проекта)
    updateRegeneration(deltaTime: number): void // основной метод регенерации
    private regenerateHealth(deltaTime: number): void // восстановление HP
    private regenerateMana(deltaTime: number): void // восстановление маны
    private isRegenerationActive(): boolean // проверка можно ли регенерировать
    
    // Обновление poison системы - теперь отключает регенерацию
    applyPoison(duration: number): void // ОТКЛЮЧАЕТ регенерацию HP на время
    ```

  - [x] **GameController.ts** - интеграция регенерации:
    ```typescript
    // В методе update() добавить
    if (this.hero) {
      this.hero.updateRegeneration(deltaTime);
    }
    ```

#### **ЧАСТЬ 4: Вампиризм**
  - [x] **Hero.ts** - добавить восстановление HP от атак:
    ```typescript
    // Вампиризм - восстановление HP после нанесения урона
    applyVampirism(damageDealt: number): void // восстанавливает HP = vampirism от урона
    ```

  - [x] **GameController.ts** - интеграция вампиризма:
    ```typescript
    // После нанесения урона крипу
    if (creepDied || heroDamage > 0) {
      this.hero.applyVampirism(heroDamage);
    }
    ```

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:**
  - [x] **Регенерация**: `this.health = Math.min(this.maxHealth, this.health + this.healthRegen*deltaTime/1000)`
  - [x] **Трата маны**: `this.energy--` при каждой атаке, пауза 2 сек если мана = 0  
  - [x] **Вампиризм**: `this.health = Math.min(this.health + this.vampirism, this.maxHealth)` после атаки
  - [x] **Отключение регенерации**: Во время атаки И когда poison активен
  - [x] **Скорости**: `healthRegen*deltaTime/1000` и `energyRegen*deltaTime/1000`

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.4:**
**Что должно работать после выполнения микрошага 2.5.4:**
- [x] **Урон героя**: Клики должны наносить урон согласно `stats["damage"]` (изначально 10), крип должен терять HP постепенно
- [x] **Трата маны**: Каждая атака должна снимать 1 ману. При мане = 0 атаки должны блокироваться
- [x] **Регенерация HP**: HP героя должно восстанавливаться согласно `stats["health-regen"]` (изначально 1/сек)
- [x] **Регенерация маны**: Мана героя должна восстанавливаться согласно `stats["mana-regen"]` (изначально 0.5/сек)
- [x] **Вампиризм**: После атаки герой должен восстанавливать HP равное `stats["vampirism"]` (изначально 0)
- [x] **Отключение регенерации**: Регенерация НЕ работает во время атаки в коллизии
- [x] **Poison эффект**: Voul теперь ОТКЛЮЧАЕТ регенерацию HP на 1 секунду
- [x] **Синхронизация полосок**: Полоски должны отображать изменения HP/маны
- [x] **Консоль**: `heroStore.getState().stats` должно показывать изменения всех характеристик в реальном времени
- **✅ Если ВСЕ характеристики работают как в старом проекте - микрошаг 2.5.4 завершен!**

### ✅ Микрошаг 2.5.5 - Система смерти героя и награждения золотом
- [x] **ПРОБЛЕМА**: Нет обработки смерти героя когда HP опускается до 0 И нет начисления золота за убийство крипов
- [x] **РЕШЕНИЕ**: Реализовать комплексную систему Game Over с подсчетом золота как в старом проекте

- [x] **Задача для AI**: Комплексное обновление GameController с системой смерти героя и золота

#### **ЧАСТЬ 1: Система награждения золотом** ✅
  - [x] **GameController.ts** - добавить систему золота:
    ```typescript
    // Новые поля для отслеживания золота
    private sessionGoldEarned: number = 0; // Золото за текущую сессию
    
    // Метод награждения золотом (вызывается в onCreepKilled)
    private awardGoldForKill(goldAmount: number, x?: number, y?: number): void {
      this.sessionGoldEarned += goldAmount;
      
      // Обновляем heroStore.stats["coins"]
      const currentStats = heroStore.getState().stats;
      const newCoins = currentStats["coins"] + goldAmount;
      
      // Синхронизируем с GoldContext и heroStore
      // Анимация монет при убийстве
    }
    
    // Геттеры
    public getSessionGoldEarned(): number { return this.sessionGoldEarned; }
    public resetSessionGold(): void { this.sessionGoldEarned = 0; }
    ```

  - [x] **Интеграция с убийством крипов**: В методе `onCreepKilled()` добавить:
    ```typescript
    // Получить goldReward из конфигурации крипа
    const creepConfig = getCreepConfig(this.currentCreep.getCreepType());
    if (creepConfig) {
      // Получаем позицию крипа для анимации монет
      const creepBounds = this.currentCreep.getBounds();
      const coinX = creepBounds.x + creepBounds.width / 2;
      const coinY = creepBounds.y + creepBounds.height / 2;
      
      this.awardGoldForKill(creepConfig.goldReward, coinX, coinY);
    }
    ```

  - [x] **CoinAnimation.ts** - создан компонент анимации монет:
    ```typescript
    // Система анимации "+X" золота при убийстве крипов
    // - Золотистый текст с черной обводкой
    // - Анимация подпрыгивания и исчезновения (1.5 сек)
    // - Иконка монеты рядом с текстом
    // - Полная интеграция с PixiJS
    ```

  - [x] **Интеграция с GoldContext** - мгновенная синхронизация UI:
    ```typescript
    // Глобальная функция updateGoldFromGameController
    // Мгновенное обновление Header при получении золота
    // Совместимость с пассивным доходом
    ```

#### **ЧАСТЬ 2: Обнаружение смерти героя** ✅
  - [x] **GameController.ts** - добавить проверку здоровья в основной цикл:
    ```typescript
    // В методе update() добавить проверку здоровья (как в старом проекте)
    if (this.hero && this.hero.getCurrentHealth() <= 0) {
      this.endGame(); // Завершаем игру, если здоровье равно 0
      return; // Останавливаем игровой цикл
    }
    ```

  - [x] **GameController.ts** - добавить метод завершения игры:
    ```typescript
    // Новый метод завершения игры (аналог из старого проекта)
    private endGame(): void {
      this.isGameRunning = false; // Останавливаем игру
      // Останавливаем все звуки и анимации
      // Показываем модальное окно Game Over с золотом сессии
      // Синхронизируем золото с сервером
    }
    ```

#### **ЧАСТЬ 3: Game Over модальное окно** ✅
  - [x] **Создать компонент**: `src/features/ui/GameOverModal/GameOverModal.tsx`
    ```typescript
    // Модальное окно Game Over с функциональностью:
    interface GameOverModalProps {
      isVisible: boolean;
      goldEarned: number; // Золото заработанное за текущую сессию
      totalGold: number;  // Общее золото героя
      onRestart: () => void;      // Начать заново (сбросить HP героя)
      onMainMenu: () => void;     // Вернуться в главное меню
    }
    ```

  - [x] **Содержимое модального окна**:
    ```
    ☠️ ГЕРОЙ ПОГИБ ☠️
    
    Заработано золота за забег: +X
    Всего золота: Y
    
    [Начать заново]  [Главное меню]
    ```

#### **ЧАСТЬ 4: Интеграция с игровым циклом** ✅
  - [x] **GamePage.tsx** - интеграция Game Over модального окна:
    ```typescript
    // Добавить состояние Game Over и передать коллбэки в GameController
    const [isGameOver, setIsGameOver] = useState(false);
    const [goldEarned, setGoldEarned] = useState(0);
    
    // Коллбэки для модального окна
    const handleRestart = () => {
      // Восстановить HP героя до максимального
      // Сбросить счетчик заработанного золота сессии
      // Перезапустить игру
    };
    
    const handleMainMenu = () => {
      // Перейти на главную страницу /
    };
    ```

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:** ✅
  - [x] **Проверка смерти**: `if (hero.health <= 0) { endGame(); return; }` в игровом цикле (game.js:94)
  - [x] **Завершение игры**: `isGameRunning = false; background.setSpeed(0); hero.stopSound(true);` (game.js:142-150)
  - [x] **Начисление золота**: `hero.addCoins(Math.floor(this.coinsEarned**1.07**(this.creepRank)));` (creep.js:280)
  - [x] **API синхронизация**: `updateGold(payload)` и `changeGold()` (creep.js:258, 281)
  - [x] **Game Over экран**: Показ статистики и кнопки меню/рестарта (закомментированный код в game.js:146-152)

#### **⚠️ ВАЖНО: Анализ синхронизации золота в старом проекте:** ✅
  - [x] **В старом проекте было ТРИ типа синхронизации:**
    1. **При каждом убийстве крипа** - `updateGold({"userId": userId, "heroId": heroId, "income": 1})` (creep.js:259-260)
    2. **При переходе между страницами** - `await closeGame()` → `endGame()` → но без прямой синхронизации золота
    3. **При инициализации приложения** - `updateGold({'userId': userId, 'heroId': heroId})` без поля income (main.js:168)

  - [x] **Современный подход для нашего проекта:**
    1. **Локальное накопление** - золото накапливается в `sessionGoldEarned` + обновляется в `heroStore.stats["coins"]`
    2. **Batch синхронизация при смерти** - отправляем весь accumulated gold одним запросом
    3. **Batch синхронизация при завершении волны** *(Фаза 3)* - когда волна пройдена успешно
  
  - [x] **API endpoint**: `/update_user_money` с payload `{"userId": number, "heroId": number, "income": number}`
  - [x] **Fallback**: Если синхронизация не удалась - сохранить в localStorage для повторной попытки

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.5:**
**Что должно работать после выполнения микрошага 2.5.5:**

**💰 Система золота:** ✅
- [x] **Золото за убийство**: После убийства крипа `stats["coins"]` должно увеличиваться на `goldReward` из конфигурации
- [x] **Разные награды**: Разные типы крипов должны давать разное количество золота согласно `creepsConfig.ts`
- [x] **Подсчет сессии**: Золото за текущую сессию должно отслеживаться отдельно
- [x] **Синхронизация GoldContext**: Золото в GoldContext должно соответствовать `heroStore.stats["coins"]`
- [x] **Синхронизация с MainPage**: Увеличение золота должно отображаться на `/`

**☠️ Система смерти:** ✅
- [x] **Смерть героя**: Когда `current-health` опускается до 0, игра должна останавливаться
- [x] **Game Over модалка**: Должно появляться модальное окно с информацией о заработанном золоте ЗА СЕССИЮ
- [x] **Кнопка "Начать заново"**: Должна восстанавливать HP героя и сбрасывать счетчик сессии
- [x] **Кнопка "Главное меню"**: Должна возвращать на MainPage `/`
- [x] **Остановка игры**: При смерти героя все звуки, анимации и игровой цикл должны останавливаться

**🔧 Техническая проверка:** ✅
- [x] **Консоль золота**: `heroStore.getState().stats["coins"]` должно увеличиваться после убийства крипа
- [x] **Консоль смерти**: При смерти героя в консоли должно логироваться "Hero died" или аналогичное сообщение  
- [x] **Локальное накопление**: `gameController.getSessionGoldEarned()` должно показывать золото текущей сессии
- [x] **API синхронизация**: При смерти героя золото сессии должно отправляться на сервер одним запросом
- [x] **Batch логика**: НЕ должно быть API запросов при каждом убийстве крипа (только локальное накопление)
- [x] **Сохранение прогресса**: Заработанное золото должно сохраняться в heroStore и после перезапуска

- **✅ Если ВСЯ система золота И смерти героя работает - микрошаг 2.5.5 завершен!**

### ✅ Микрошаг 2.5.6 - Система паузы и схемы взаимодействия с пользователем
- [x] **ПРОБЛЕМА**: Нет системы паузы игры и четкой схемы взаимодействия при переходах между вкладками
- [x] **РЕШЕНИЕ**: Реализовать полную систему паузы и модальных окон для навигации

- [x] **Задача для AI**: Создать систему паузы и схему взаимодействия пользователя с игрой

#### **🎮 СХЕМА ВЗАИМОДЕЙСТВИЯ ПОЛЬЗОВАТЕЛЯ С ИГРОЙ:**

#### **СЦЕНАРИЙ 1: Смерть героя + "Начать заново"** ✅
  - [x] **Пользователь умирает** → Модальное окно с надписями типа герой погиб, сколько успел золота заработать и кнопки начать заново и выйти в главное меню
  - [x] **Нажимает "Начать заново"**:
    1. ✅ **Золото сохраняется** - заработанное золото остается в `heroStore.stats["coins"]`
    2. ✅ **Игра сбрасывается к исходному состоянию уровня** - HP/мана снова максимальны
    3. ✅ **Игра сбрасывается** - герой снова бежит, новый крип появляется за экраном
    4. ✅ **Счетчик золота сбрасывается** - новый подсчет для новой сессии

#### **СЦЕНАРИЙ 2: Переход на другую вкладку во время игры**
  - [ ] **Пользователь кликает на другую вкладку** (главная/магазин) во время игры
  - [ ] **Игра автоматически ставится на паузу** 
  - [ ] **Появляется модальное окно подтверждения**:
    ```
    ⚠️ ПОКИНУТЬ ИГРУ?
    
    Вы уверены, что хотите покинуть игру?
    
    ✅ Заработанное золото за этот забег будет начислено
    ⚠️ Текущий прогресс будет потерян - придется начать заново
    
    [Да, покинуть]  [Остаться в игре]
    ```
  - [ ] **Если "Да, покинуть"**:
    1. ✅ **Золото сохраняется** - накопленное за сессию золото начисляется
    2. ✅ **Синхронизация с сервером** - отправляется batch запрос
    3. ✅ **Переход на выбранную вкладку**
    4. ✅ **Игра сбрасывается** - при возврате игра начнется заново
  - [ ] **Если "Остаться в игре"**:
    1. ✅ **Модалка закрывается**
    2. ✅ **Игра снимается с паузы** 
    3. ✅ **Продолжение игры** с того же места

#### **ЧАСТЬ 1: Система паузы игры** ✅
  - [x] **GameController.ts** - добавить методы паузы:
    ```typescript
    // Основные методы паузы
    public pauseGame(): void // ставит игру на паузу
    public resumeGame(): void // снимает игру с паузы
    public isPaused(): boolean // проверка состояния паузы
    
    // Управление игровым циклом
    private isGameRunning: boolean = true; // флаг работы игры
    private isPausedByUser: boolean = false; // флаг паузы пользователем
    
    // Обновленный метод update
    public update(deltaTime: number): void {
      if (!this.isGameRunning || this.isPausedByUser) {
        return; // Пропускаем обновление если игра на паузе
      }
      // ... существующий код update
    }
    ```

  - [x] **Интеграция с PixiJS Ticker** - правильная пауза анимаций:
    ```typescript
    // В GameController при паузе
    pauseGame(): void {
      this.isPausedByUser = true;
      // Остановить все анимации PixiJS
      if (this.hero) this.hero.pauseAnimations();
      if (this.currentCreep) this.currentCreep.pauseAnimations();
      // Остановить звуки
      // Показать overlay паузы
    }
    ```

#### **ЧАСТЬ 2: Модальное окно подтверждения навигации**
  - [ ] **Создать компонент**: `src/features/ui/NavigationConfirmModal/NavigationConfirmModal.tsx`
    ```typescript
    interface NavigationConfirmModalProps {
      isVisible: boolean;
      targetTab: string; // 'main' | 'shop'
      sessionGold: number; // Золото текущей сессии
      onConfirm: () => void; // Подтвердить переход
      onCancel: () => void;  // Остаться в игре
    }
    ```

#### **ЧАСТЬ 3: Интеграция с навигацией**
  - [ ] **App.tsx / GamePage.tsx** - перехват навигации:
    ```typescript
    // Обработчик попытки навигации
    const handleNavigationAttempt = (targetRoute: string) => {
      if (isGameActive && !isPaused) {
        // Поставить игру на паузу
        gameController.pauseGame();
        // Показать модальное окно подтверждения
        setNavigationModal({
          isVisible: true,
          targetTab: targetRoute,
          sessionGold: gameController.getSessionGoldEarned()
        });
      } else {
        // Обычная навигация если игра не активна
        navigate(targetRoute);
      }
    };
    ```

#### **ЧАСТЬ 4: Кнопка паузы (временно)** ✅
  - [x] **Добавить кнопку паузы в верхнее меню** (как в старом проекте):
    ```typescript
    // Интеграция с кнопкой настроек в Header.tsx
    // При клике на настройки - игра ставится на паузу
    // При закрытии настроек - игра снимается с паузы
    ```
  - [x] **РЕАЛИЗОВАНО**: 
    - Создан `GameContext` для централизованного управления паузой
    - Кнопка настроек в Header теперь управляет паузой игры  
    - При открытии настроек - игра автоматически на паузе
    - При закрытии настроек - игра автоматически возобновляется
    - Иконка остается шестеренкой (настройки), готова к добавлению модального окна
    - Исправлен баг с кликами во время паузы - теперь герой не атакует на паузе

#### **СПРАВКА ИЗ СТАРОГО ПРОЕКТА:** ✅
  - [x] **Пауза игры**: `isGameRunning = false` останавливает `requestAnimationFrame` (game.js:82-96)
  - [x] **Управление звуками**: `hero.stopSound()` и `hero.resumeSound()` (game.js:115, 125)
  - [x] **Кнопки паузы**: `pause-button` и `resume-button` с обработчиками (game.js:183-220)
  - [x] **Переходы между вкладками**: `closeGame()` вызывается перед навигацией (main.js:100)

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА МИКРОШАГА 2.5.6:**
**Что должно работать после выполнения микрошага 2.5.6:**

**⏸️ Система паузы:**
- [x] **Кнопка паузы**: В верхнем меню кнопка настроек ⚙️ управляет паузой игры
- [x] **Пауза работает**: При открытии настроек игра останавливается (герой, крип, анимации, фон)
- [x] **Снятие паузы**: При закрытии настроек игра возобновляется с того же места
- [x] **Блокировка кликов**: Во время паузы клики по экрану не вызывают атаки героя
- [x] **GameContext**: Централизованное управление паузой через React Context

**🔄 Навигация во время игры:** ⏳ (СЛЕДУЮЩИЙ ЭТАП)
- [ ] **ЧАСТЬ 2: Модальное окно подтверждения навигации** - НЕ РЕАЛИЗОВАНО
- [ ] **ЧАСТЬ 3: Интеграция с навигацией** - НЕ РЕАЛИЗОВАНО  
- [ ] **Перехват навигации**: При клике на другую вкладку во время игры должна появляться модалка
- [ ] **Модалка подтверждения**: Должно появляться окно с предупреждением о потере прогресса
- [ ] **"Да, покинуть"**: Золото должно начисляться, игра сбрасываться, переход на выбранную вкладку
- [ ] **"Остаться"**: Модалка закрывается, игра продолжается
- [ ] **Автопауза**: Игра автоматически ставится на паузу при появлении модалки

**🎮 Сценарии взаимодействия:**
- [ ] **Смерть + перезапуск**: Золото сохраняется, герой восстанавливается, игра начинается заново
- [ ] **Навигация + возврат**: При возврате в игру после навигации игра должна начинаться заново
- [ ] **Сохранение прогресса**: Заработанное золото должно сохраняться во всех сценариях

**🔧 Техническая проверка:** ✅
- [x] **Консоль паузы**: При паузе в консоли должно логироваться "Game paused"
- [x] **Консоль навигации**: При попытке навигации должно логироваться предупреждение
- [x] **Состояние игры**: `gameController.isPaused()` должно корректно отражать состояние
- [x] **Синхронизация**: При принудительном выходе золото должно синхронизироваться с сервером

- **🟡 ЧАСТИЧНО ЗАВЕРШЕН - Система паузы работает, навигационные модалки - следующий этап**

#### **✅ ЧТО ЗАВЕРШЕНО В МИКРОШАГЕ 2.5.6:**
- ✅ **Полная система паузы игры** - готова к работе
- ✅ **Интеграция с кнопкой настроек** - элегантное решение вместо отдельной кнопки
- ✅ **GameContext** - централизованное управление состоянием игры
- ✅ **Блокировка кликов на паузе** - исправлен важный баг
- ✅ **Готовность к модальным окнам** - инфраструктура создана
- ✅ **Модальное окно настроек** - с ползунком звука и чекбоксом подсказок
- ✅ **Стилизация модального окна** - в едином стиле с GameOverModal

#### **⏳ ЧТО ОСТАЛОСЬ НА СЛЕДУЮЩИЙ ЭТАП:**
- ⏳ Модальное окно подтверждения навигации
- ⏳ Перехват попыток навигации 
- ⏳ Интеграция с роутингом приложения

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 2.5:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 2.5:**

**🎮 Тестирование игры `/game`:** ✅
- [x] **Характеристики героя**: Открыть консоль браузера → `heroStore.getState().stats` должно показать все поля включая `level`, `coins`, `current-health`, `current-mana` ✅
- [x] **Крипы с HP**: Крипы должны иметь полоски здоровья и НЕ умирать от одного клика ✅
- [x] **Урон героя**: Клики должны наносить урон согласно `stats["damage"]` (изначально 10), крип должен терять HP постепенно 
- [x] **Атаки крипов**: При коллизии крип должен атаковать героя и уменьшать его `current-health`
- [x] **Трата маны**: Каждая атака должна снимать 1 ману. При мане = 0 атаки должны блокироваться
- [x] **Регенерация HP**: HP героя должно восстанавливаться со временем согласно `health-regen` (1/сек)
- [x] **Регенерация маны**: Мана героя должна восстанавливаться со временем согласно `mana-regen` (0.5/сек)
- [x] **Вампиризм**: После атаки герой должен восстанавливать HP равное `vampirism` (изначально 0)
- [x] **Золото за убийство**: За убийство крипа должно добавляться золото в `stats["coins"]`
- [x] **Особые способности**: 
  - Satyr должен отнимать ману при атаке (manaburn)
  - Voul должен отключать регенерацию HP на 1 секунду (poison)

**📊 Тестирование MainPage `/`:** ✅
- [x] Должны отображаться ВСЕ характеристики включая `level`, `coins`, `current-health`, `current-mana`
- [x] Значения должны соответствовать тем что в игре (синхронизация)
- [x] После игры и получения золота - на MainPage должно отображаться обновленное количество `coins`

**🔧 Техническая проверка:** ✅
- [x] Консоль браузера: НЕТ ошибок связанных с heroStore или характеристиками ✅
- [x] `heroStore.getState().stats` содержит все 12 полей ✅
- [x] `GoldContext` и `heroStore.stats["coins"]` синхронизированы
- [x] Разные типы крипов имеют разные HP, урон и награды ✅
- [x] Полоски здоровья работают для крипов и героя ✅
- [x] Крипы имеют реальное здоровье и получают урон ✅

**✅ Если все пункты выполнены - Фаза 2.5 завершена успешно!**

---

## 📋 Фаза 3: Основы уровневой системы (30 уровней)

### ✅ Микрошаг 3.1 - Система уровней героя (1-30) ✅ **ЗАВЕРШЕН**
  - [x] **Задача для AI**: Создать `src/game/systems/HeroLevelSystem.ts` (папка systems пустая)
  - [x] **ЧАСТЬ 1**: Базовый класс HeroLevelSystem:
    ```typescript
    class HeroLevelSystem {
      getCurrentLevel(): number;     // Текущий уровень (1-30)
      levelUp(): void;              // Повысить уровень
      getMaxLevel(): number;        // Максимум 30
      canLevelUp(): boolean;        // Можно ли повыситься
      getLevelName(): string;       // "Бронза", "Серебро", "Платина"
    }
    ```
  - [x] **ЧАСТЬ 2**: Градации уровней:
    - 1-5: Бронза
    - 6-10: Серебро  
    - 11-15: Золото
    - 16-20: Платина
    - 21-25: Мастер
    - 26-30: Грандмастер
  - [x] **ЧАСТЬ 3**: Сохранение в localStorage + события через EventEmitter

#### 🔍 **ПРОВЕРКА МИКРОШАГА 3.1:**
- [x] Консоль браузера: `new HeroLevelSystem().getCurrentLevel()` возвращает 1
- [x] `levelUp()` → `getCurrentLevel()` возвращает 2
- [x] `getLevelName()` возвращает правильную градацию
- [x] После перезагрузки страницы уровень сохраняется

### ✅ Микрошаг 3.2 - Счетчик крипов на уровне (9+1 босс) ✅ **ЗАВЕРШЕН**
  - [x] **Задача для AI**: Модификация GameController для отслеживания прогресса уровня
  - [x] **ЧАСТЬ 1**: Новые переменные в GameController:
    ```typescript
    private currentLevelProgress: number = 0;  // Убито крипов на уровне (0-10)
    private isCurrentlyBoss: boolean = false;  // Сейчас идет босс
    private heroLevelSystem: HeroLevelSystem;  // Ссылка на систему уровней
    ```
  - [x] **ЧАСТЬ 2**: Логика подсчета в методе `onCreepKilled()`:
    - Увеличить `currentLevelProgress`
    - Если `currentLevelProgress === 9` → следующий крип будет босс
    - Если `currentLevelProgress === 10` → уровень завершен
  - [x] **ЧАСТЬ 3**: Метод завершения уровня:
    ```typescript
    private onLevelComplete(): void {
      this.heroLevelSystem.levelUp();
      this.currentLevelProgress = 0;
      // Показать модальное окно (позже)
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 3.2:**
- [x] Консоль браузера: логи "Убит крип 1/10", "Убит крип 2/10" и т.д.
- [x] После 9-го крипа лог "Следующий крип - БОСС!"
- [x] После 10-го крипа лог "Уровень завершен! Переход на уровень X"

### ✅ Микрошаг 3.3 - UI отображение прогресса уровня ✅
- [x] **Задача для AI**: Создать `src/features/ui/LevelProgressHUD/LevelProgressHUD.tsx`
  - [x] **ЧАСТЬ 1**: Компонент отображения:
    - "Уровень 1 (Бронза)"
    - "Прогресс: 3/10"
    - "БОСС!" (когда идет босс)
  - [x] **ЧАСТЬ 2**: Стилизация (угол экрана, не мешает игре)
  - [x] **ЧАСТЬ 3**: Интеграция с GameController и HeroLevelSystem

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 3:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 3:**
- [x] Запустить игру `/game`
- [x] В углу экрана: "Уровень 1 (Бронза)" и "Прогресс: 0/10"
- [x] После каждого убийства крипа прогресс увеличивается: "1/10", "2/10" и т.д.
- [x] После 9-го крипа: "Прогресс: 9/10"
- [x] 10-й крип должен быть БОССОМ и показывать "БОСС!"
- [x] После убийства босса: "Уровень 2 (Бронза)" и "Прогресс: 0/10"
- [x] Проверить localStorage: уровень сохраняется при перезагрузке
- **✅ ФАЗА 3 ЗАВЕРШЕНА!**

---

## 📋 Фаза 4: Прогрессия крипов и боссов

### ✅ Микрошаг 4.1 - Конфигурация уровней ✅
- [x] **Задача для AI**: Создать `src/game/config/levelsConfig.ts`
  - [x] **ЧАСТЬ 1**: Интерфейс конфигурации уровня:
    ```typescript
    interface LevelConfig {
      level: number;                    // Номер уровня (1-30)
      normalCreeps: string[];           // Крипы в первой девятке
      bossCreep: string;               // Босс на 10-й позиции
      newCreepIntroduced?: string;     // Новый крип, который вводится как босс
      description: string;             // Описание уровня
    }
    ```
  - [x] **ЧАСТЬ 2**: Конфигурация всех 30 уровней:
    - **Уровень 1**: normalCreeps: ["satyr", "wolf"], boss: "shishka"
    - **Уровень 2**: normalCreeps: ["satyr", "wolf", "shishka"], boss: "direCreep"  
    - **Уровень 3**: normalCreeps: ["satyr", "wolf", "shishka", "direCreep"], boss: "voul"
    - И так далее до 30 уровня...
  - [x] **ЧАСТЬ 3**: Функции для работы с конфигурацией:
    ```typescript
    getLevelConfig(level: number): LevelConfig;
    getAvailableCreepsForLevel(level: number): string[];
    getBossForLevel(level: number): string;
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 4.1:**
- [x] Консоль: `getLevelConfig(1)` возвращает правильную конфигурацию
- [x] `getAvailableCreepsForLevel(1)` возвращает ["satyr", "wolf"]
- [x] `getBossForLevel(1)` возвращает "shishka"

### ✅ Микрошаг 4.2 - Интеграция прогрессии в GameController ✅
- [x] **Задача для AI**: Модификация GameController для использования конфигурации уровней
  - [x] **ЧАСТЬ 1**: Изменение метода `getCreepType()`:
    ```typescript
    private getCreepType(): string {
      const currentLevel = heroLevelSystem.getCurrentLevel();
      const levelConfig = getLevelConfig(currentLevel);
      
      if (this.currentLevelProgress === 9) {
        // 10-й крип - босс
        return levelConfig.bossCreep;
      } else {
        // 1-9 крипы - случайный из доступных
        const availableCreeps = levelConfig.normalCreeps;
        return availableCreeps[Math.floor(Math.random() * availableCreeps.length)];
      }
    }
    ```
  - [x] **ЧАСТЬ 2**: Модификация создания босса:
    - Увеличенный масштаб (x1.5)
    - Увеличенное здоровье (x3)
    - Визуальная индикация "БОСС" в логах
  - [x] **ЧАСТЬ 3**: Обновление логов с информацией об уровне

#### 🔍 **ПРОВЕРКА МИКРОШАГА 4.2:**
- [x] На уровне 1: первые 9 крипов - только Сатир и Волк (случайно)
- [x] 10-й крип на уровне 1 - Шишка-босс (увеличенная, больше HP)
- [x] На уровне 2: первые 9 крипов - Сатир, Волк, Шишка (случайно)
- [x] 10-й крип на уровне 2 - direCreep-босс

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 4:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 4:**
- [x] Запустить игру с 1 уровня
- [x] Уровень 1: встречаются только Сатир и Волк, босс - увеличенная Шишка
- [x] Уровень 2: встречаются Сатир, Волк, Шишка, босс - увеличенный direCreep
- [x] Прогрессия работает правильно: каждый побежденный босс становится обычным крипом
- **✅ ФАЗА 4 ЗАВЕРШЕНА!**

---

## 📋 Фаза 4.5: Улучшение визуального оформления HUD уровня

### ✅ Микрошаг 4.5.1 - Система показа значка уровня
- [ ] **Задача для AI**: Создать `src/game/components/LevelDisplaySystem.ts`
  - [ ] **ЧАСТЬ 1**: Компонент показа значка уровня:
    ```typescript
    class LevelDisplaySystem {
      showLevelIcon(level: number): void;     // Показать значок уровня на 3 секунды
      showProgressBar(): void;               // Показать полоску прогресса
      updateProgress(current: number): void;  // Обновить позицию джаггернаута
      hideLevelDisplay(): void;              // Скрыть отображение
    }
    ```
  - [ ] **ЧАСТЬ 2**: Загрузка ресурсов:
    - Значки уровней из папки `/media/game/assets/levels/` (называются как номера уровней)
    - Иконка джаггернаута `/media/game/assets/heroes/Juggernaut_minimap_icon.webp`
  - [ ] **ЧАСТЬ 3**: Отображение значка с номером уровня:
    - Значок уровня по центру сверху экрана
    - Цифра номера уровня поверх значка (белый цвет с черной обводкой для видимости)
    - Показ на 3 секунды

#### 🔍 **ПРОВЕРКА МИКРОШАГА 4.5.1:**
- [ ] При входе в игру появляется значок текущего уровня с цифрой посередине
- [ ] Цифра хорошо видна на фоне значка (белый цвет с обводкой)
- [ ] Значок отображается ровно 3 секунды
- [ ] После 3 секунд значок исчезает

### ✅ Микрошаг 4.5.2 - Полоска прогресса с движущимся джаггернаутом
- [ ] **Задача для AI**: Реализация интерактивной полоски прогресса
  - [ ] **ЧАСТЬ 1**: Дизайн полоски прогресса:
    ```typescript
    // Полоска должна иметь:
    - 9 делений (10 границ соответственно)
    - Общая длина адаптивная к экрану
    - Деления четко видны (линии или точки)
    - Фон полоски (темно красный)
    ```
  - [ ] **ЧАСТЬ 2**: Иконка джаггернаута:
    - Размещается поверх полоски прогресса
    - Начальная позиция - первая граница (слева)
    - Плавная анимация перемещения между границами
    - Размер иконки адаптивный
  - [ ] **ЧАСТЬ 3**: Логика перемещения:
    ```typescript
    // При убийстве крипа:
    updateProgress(currentCreep: number): void {
      // currentCreep = 1-10
      // Позиция джаггернаута = граница currentCreep (1-10)
      // Анимация перемещения к новой позиции
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 4.5.2:**
- [ ] После исчезновения значка уровня появляется полоска прогресса
- [ ] Полоска имеет 9 делений (10 границ)
- [ ] Иконка джаггернаута находится на первой границе (слева)
- [ ] При убийстве первого крипа джаггернаут плавно перемещается на вторую границу
- [ ] При убийстве каждого следующего крипа джаггернаут движется дальше

### ✅ Микрошаг 4.5.3 - Интеграция с GameController и переходами между уровнями
- [ ] **Задача для AI**: Полная интеграция новой системы отображения
  - [ ] **ЧАСТЬ 1**: Интеграция с GameController:
    ```typescript
    // В GameController.ts добавить:
    private levelDisplaySystem: LevelDisplaySystem;
    
    // При старте нового уровня:
    private startNewLevel(): void {
      this.levelDisplaySystem.showLevelIcon(currentLevel);
      // Через 3 секунды показать полоску прогресса
      setTimeout(() => {
        this.levelDisplaySystem.showProgressBar();
      }, 3000);
    }
    
    // При убийстве крипа:
    private onCreepKilled(): void {
      this.currentLevelProgress++;
      this.levelDisplaySystem.updateProgress(this.currentLevelProgress);
      // ... остальная логика
    }
    ```
  - [ ] **ЧАСТЬ 2**: Замена старого LevelProgressHUD:
    - Удалить или скрыть старый текстовый HUD "Уровень X, Прогресс: Y/10"
    - Использовать новую визуальную систему вместо текста
    - Сохранить функциональность, улучшить визуал
  - [ ] **ЧАСТЬ 3**: Обработка завершения уровня:
    ```typescript
    // При завершении уровня (убийство босса):
    private onLevelComplete(): void {
      this.levelDisplaySystem.hideLevelDisplay();
      // Показать модальное окно завершения уровня
      // После закрытия модалки - startNewLevel() для нового уровня
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 4.5.3:**
- [ ] Старый текстовый HUD больше не отображается
- [ ] При переходе на новый уровень цикл повторяется: значок → полоска
- [ ] Джаггернаут корректно сбрасывается на первую позицию нового уровня
- [ ] Анимации работают плавно и не конфликтуют с другими элементами

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 4.5:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 4.5:**
- [ ] **Запуск игры**: Появляется красивый значок текущего уровня с цифрой посередине
- [ ] **Через 3 секунды**: Значок исчезает, появляется полоска прогресса с джаггернаутом слева
- [ ] **Убийство крипов**: При каждом убийстве джаггернаут плавно перемещается к следующей границе
- [ ] **9-й крип**: Джаггернаут находится на предпоследней границе
- [ ] **10-й крип (босс)**: Джаггернаут достигает последней границы
- [ ] **Завершение уровня**: Полоска исчезает, показывается модальное окно
- [ ] **Новый уровень**: Цикл повторяется с новым значком уровня
- [ ] **Адаптивность**: Все элементы корректно масштабируются на разных экранах
- [ ] **Плавность**: Все анимации работают без рывков и задержек
- **✅ Если новая визуальная система уровней работает полностью - Фаза 4.5 завершена!**

---

## 📋 Фаза 5: Модальные окна и переходы между уровнями

### ✅ Микрошаг 5.1 - Модальное окно завершения уровня
- [ ] **Задача для AI**: Создать `src/features/ui/LevelCompleteModal/LevelCompleteModal.tsx`
  - [ ] **ЧАСТЬ 1**: Дизайн модального окна:
    ```
    🎉 ПОЗДРАВЛЯЕМ! 🎉
    
    Уровень повышен!
    Герой: Уровень 2 (Бронза)
    
    Заработано золота: +45
    Всего золота: 123
    
    [Продолжить]
    ```
  - [ ] **ЧАСТЬ 2**: Стилизация в стиле GameOverModal (единообразие)
  - [ ] **ЧАСТЬ 3**: Анимация появления и исчезновения

#### 🔍 **ПРОВЕРКА МИКРОШАГА 5.1:**
- [ ] После убийства босса появляется модальное окно
- [ ] Показывает правильный новый уровень
- [ ] Показывает заработанное золото за уровень
- [ ] Кнопка "Продолжить" закрывает модальное окно

### ✅ Микрошаг 5.2 - Интеграция модального окна с игрой
- [ ] **Задача для AI**: Интеграция LevelCompleteModal с GameController и GamePage
  - [ ] **ЧАСТЬ 1**: Добавление состояния в GamePage:
    ```typescript
    const [isLevelComplete, setIsLevelComplete] = useState(false);
    const [levelCompleteData, setLevelCompleteData] = useState(null);
    ```
  - [ ] **ЧАСТЬ 2**: Callback в GameController при завершении уровня:
    ```typescript
    private onLevelCompleteCallback?: (data: LevelCompleteData) => void;
    
    setLevelCompleteCallback(callback: (data: LevelCompleteData) => void): void;
    ```
  - [ ] **ЧАСТЬ 3**: Пауза игры при показе модального окна
  - [ ] **ЧАСТЬ 4**: Возобновление игры после закрытия модального окна

#### 🔍 **ПРОВЕРКА МИКРОШАГА 5.2:**
- [ ] При завершении уровня игра автоматически ставится на паузу
- [ ] Модальное окно появляется и блокирует игру
- [ ] После закрытия модального окна игра возобновляется
- [ ] Новый уровень начинается с правильной конфигурацией крипов

### ✅ Микрошаг 5.3 - Сохранение прогресса и синхронизация
- [ ] **Задача для AI**: Сохранение состояния игры между уровнями
  - [ ] **ЧАСТЬ 1**: Сохранение уровня героя в localStorage при каждом повышении
  - [ ] **ЧАСТЬ 2**: Сохранение накопленного золота за уровень
  - [ ] **ЧАСТЬ 3**: Синхронизация с сервером при завершении уровня (batch отправка)
  - [ ] **ЧАСТЬ 4**: Восстановление состояния при возврате в игру

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 5:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 5:**
- [ ] Пройти полностью 1-й уровень (9 крипов + босс)
- [ ] Должно появиться модальное окно "Поздравляем! Уровень повышен!"
- [ ] После закрытия модального окна начинается 2-й уровень
- [ ] Конфигурация крипов соответствует 2-му уровню
- [ ] При перезагрузке страницы уровень и прогресс сохраняются
- [ ] Золото правильно накапливается и синхронизируется
- **✅ Если система переходов между уровнями работает - Фаза 5 завершена!**

---

## 📋 Фаза 6: Главная страница с анимацией героя

### ✅ Микрошаг 6.1 - Анимация героя на главной странице
- [ ] **Задача для AI**: Модификация MainPage для отображения анимированного героя
  - [ ] **ЧАСТЬ 1**: Интеграция PixiJS на главную страницу:
    ```typescript
    // В MainPage.tsx добавить канвас для героя
    const heroCanvasRef = useRef<HTMLCanvasElement>(null);
    ```
  - [ ] **ЧАСТЬ 2**: Создание упрощенного HeroDisplay компонента:
    - Анимация idle (фронтальная стойка)
    - Без интерактивности
    - Масштабирование под размер экрана
  - [ ] **ЧАСТЬ 3**: Размещение героя в центре экрана

#### 🔍 **ПРОВЕРКА МИКРОШАГА 6.1:**
- [ ] На главной странице отображается анимированный Джаггернаут
- [ ] Анимация idle работает плавно
- [ ] Герой масштабируется правильно на разных экранах
- [ ] Нет конфликтов с другими элементами страницы

### ✅ Микрошаг 6.2 - Красивое отображение характеристик
- [ ] **Задача для AI**: Редизайн отображения характеристик героя на главной странице
  - [ ] **ЧАСТЬ 1**: Новый компонент `src/features/ui/HeroStatsDisplay/HeroStatsDisplay.tsx`:
    ```
    ТОЛЬКО БАЗОВЫЕ ХАРАКТЕРИСТИКИ:
    ❤️ Макс. здоровье: 1,250
    🩹 Регенерация ХП: +5.2/с
    💙 Макс. мана: 180
    ⚡ Регенерация МП: +3.1/с
    ⚔️ Урон: 180
    🩸 Вампиризм: 25%
    🏃 Скорость движения: 350
    ```
  - [ ] **ЧАСТЬ 2**: Информация в других местах:
    - **Header**: Золото + пассивный доход (уже есть)
    - **Рядом с героем**: ⭐ LVL 25 (в углу портрета)
    - **Характеристики**: Только статы для улучшения в магазине
  - [ ] **ЧАСТЬ 3**: Компактное расположение под героем с твоей цветовой схемой

#### 🔍 **ПРОВЕРКА МИКРОШАГА 6.2:**
- [ ] Характеристики отображаются только базовые (без дублирования)
- [ ] Уровень показывается в углу портрета героя как ⭐ LVL 25
- [ ] Золото и доход остаются в Header (не дублируются)
- [ ] Используется цветовая схема проекта (#1c2028, градиенты, золотой)
- [ ] Данные синхронизируются с реальными характеристиками из heroStore

### ✅ Микрошаг 6.3 - Боковые кнопки и навигация
- [ ] **Задача для AI**: Добавление дополнительных элементов UI на главную страницу
  - [ ] **ЧАСТЬ 1**: Левые боковые кнопки:
    - 🎮 ИГРА
    - ⚔️ БИТВА (заглушка)
    - 👑 РЕЙТИНГ (заглушка)
    - 🛡️ ГИЛЬДИИ (заглушка)
  - [ ] **ЧАСТЬ 2**: Правые боковые кнопки:
    - 🛒 МАГАЗИН
    - 🎒 ИНВЕНТАРЬ (заглушка)
    - 🏆 ДОСТИЖЕНИЯ (заглушка)
  - [ ] **ЧАСТЬ 3**: Панель выбора героя + заглушка пассивной способности
  - [ ] **ЧАСТЬ 4**: Адаптивный дизайн с твоей цветовой схемой

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 6:**
**Что должно быть видно на экране после выполнения всех шагов Фазы 6:**
- [ ] Открыть главную страницу `/`
- [ ] В центре - анимированный Джаггернаут в стойке idle с ⭐ LVL 25 в углу
- [ ] Под героем - панель с ТОЛЬКО базовыми характеристиками (без дублирования)
- [ ] Header показывает золото и пассивный доход (не дублируется в характеристиках)
- [ ] Боковые кнопки навигации: ИГРА, МАГАЗИН, ИНВЕНТАРЬ, ДОСТИЖЕНИЯ
- [ ] Панель выбора героя: Джаггернаут активен, остальные заблокированы
- [ ] Заглушка пассивной способности
- [ ] Используется цветовая схема проекта (#1c2028, золотые градиенты)
- [ ] Все данные синхронизируются с heroStore
- [ ] Дизайн адаптируется под размер экрана
- **✅ Если главная страница выглядит красиво, логично и информативно - Фаза 6 завершена!**

---

## 📋 Фаза 7: Система конфигурации героев и добавление Кентавра

### ✅ Микрошаг 7.1 - Создание системы конфигов героев
- [ ] **Задача для AI**: Создать `src/game/config/heroConfig.ts` по аналогии с `creepsConfig.ts`
  - [ ] **ЧАСТЬ 1**: Определить интерфейсы:
    ```typescript
    interface HeroTypeConfig {
      id: string;                     // "juggernaut", "centaur"
      name: string;                   // "Джаггернаут", "Кентавр"
      
      // Визуальные параметры
      scale: number;                  // Базовый масштаб (0.3 для juggernaut)
      positionX: number;              // Позиция по X (0.2 = 20% от края)
      positionY: number;              // Позиция по Y (0.7 = 70% от верха)
      
      // Конфигурация спрайт-листов
      animations: {
        idle: SpriteSheetConfig;
        run: SpriteSheetConfig;
        attack: SpriteSheetConfig;
      };
      
      // Звуковые эффекты
      sounds?: {
        run?: string;
        attack?: string[];
      };
      
      // Особенности анимаций
      animationSpeeds?: {
        idle?: number;
        run?: number; 
        attack?: number;
      };
    }
    ```
  - [ ] **ЧАСТЬ 2**: Создать конфиг Джаггернаута:
    - Перенести данные из AssetsManager
    - Использовать существующие настройки из GAME_CONFIG.HERO
  - [ ] **ЧАСТЬ 3**: Добавить функции для работы с конфигами:
    ```typescript
    export function getHeroConfig(heroId: string): HeroTypeConfig;
    export function getAllHeroTypes(): string[];
    export function getHeroAnimationConfig(heroId: string, animation: string);
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 7.1:**
- [ ] Файл `src/game/config/heroConfig.ts` создан
- [ ] Консоль: `getHeroConfig('juggernaut')` возвращает полную конфигурацию
- [ ] Консоль: `getAllHeroTypes()` возвращает `['juggernaut']`
- [ ] Нет ошибок загрузки при импорте heroConfig

### ✅ Микрошаг 7.2 - Анализ спрайт-листов Кентавра
- [ ] **Задача для AI**: Определить параметры спрайт-листов Кентавра
  - [ ] **ЧАСТЬ 1**: Анализ файлов в `public/media/game/assets/heroes/`:
    - `centaur_idle.webp` - определить frameWidth, frameHeight, framesX, framesY, totalFrames
    - `centaur_run.webp` - аналогично
    - `centaur_attack.webp` - аналогично
  - [ ] **ЧАСТЬ 2**: Создать конфигурацию Кентавра:
    - Использовать анализ из ЧАСТИ 1
    - Настроить масштаб (возможно больше чем у Джаггернаута)
    - Настроить позицию (возможно ниже, так как Кентавр массивнее)
  - [ ] **ЧАСТЬ 3**: Добавить в `HERO_TYPES`:
    ```typescript
    centaur: {
      id: 'centaur',
      name: 'Кентавр Завоеватель', 
      scale: 0.4,              // Больше Джаггернаута
      positionY: 0.75,         // Ниже (более массивный)
      animations: { /* определенные параметры */ }
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 7.2:**
- [ ] Консоль: `getHeroConfig('centaur')` возвращает корректную конфигурацию
- [ ] Консоль: `getAllHeroTypes()` возвращает `['juggernaut', 'centaur']`
- [ ] Параметры спрайт-листов соответствуют реальным файлам
- [ ] Масштаб и позиция настроены логично для массивного героя

### ✅ Микрошаг 7.3 - Интеграция героев с AssetsManager
- [ ] **Задача для AI**: Обновить `src/game/managers/AssetsManager.ts` для использования `heroConfig.ts`
  - [ ] **ЧАСТЬ 1**: Модификация `createAssetManifest()`:
    - Заменить хардкод секции `heroes` на динамическую генерацию
    - Использовать `getAllHeroTypes()` и `getHeroConfig()`
    - Генерировать манифест для всех героев автоматически
  - [ ] **ЧАСТЬ 2**: Обновление методов загрузки:
    - `loadHeroAssets()` должен работать с новой структурой
    - Сохранить обратную совместимость с существующими методами
  - [ ] **ЧАСТЬ 3**: Тестирование загрузки:
    - Убедиться что Джаггернаут загружается как раньше
    - Проверить загрузку ресурсов Кентавра

#### 🔍 **ПРОВЕРКА МИКРОШАГА 7.3:**
- [ ] Игра `/game` запускается без ошибок с Джаггернаутом
- [ ] Консоль браузера: нет ошибок загрузки ресурсов
- [ ] Консоль: `assetsManager.getHeroTexture('centaur', 'idle')` возвращает текстуру
- [ ] Консоль: `assetsManager.getHeroTexture('juggernaut', 'idle')` работает как раньше

### ✅ Микрошаг 7.4 - Простое переключение героев в игре
- [ ] **Задача для AI**: Добавить возможность смены героя в GameController
  - [ ] **ЧАСТЬ 1**: Метод смены героя в `GameController`:
    ```typescript
    public async switchHero(heroType: string): Promise<void> {
      // Удалить текущего героя со сцены
      // Создать нового героя с новым типом
      // Добавить на сцену с сохранением позиции
      // Восстановить коллбэки и связи
    }
    ```
  - [ ] **ЧАСТЬ 2**: Конструктор `Hero` принимает heroType:
    - Загружать конфигурацию через `getHeroConfig(heroType)`
    - Применять настройки масштаба и позиции из конфига
    - Использовать правильные анимации
  - [ ] **ЧАСТЬ 3**: Глобальные функции для тестирования:
    ```typescript
    // Делаем доступными в консоли браузера
    (window as any).switchToJuggernaut = () => gameController.switchHero('juggernaut');
    (window as any).switchToCentaur = () => gameController.switchHero('centaur'); 
    (window as any).listHeroes = () => getAllHeroTypes();
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 7.4:**
- [ ] Запустить игру `/game`
- [ ] Консоль: `switchToJuggernaut()` - отображается Джаггернаут с правильным масштабом
- [ ] Консоль: `switchToCentaur()` - отображается Кентавр с правильным масштабом
- [ ] Консоль: `listHeroes()` - показывает оба героя  
- [ ] При переключении сохраняются анимации и реакция на клики
- [ ] Кентавр крупнее и ниже Джаггернаута (согласно конфигу)

### ✅ Микрошаг 7.5 - Тонкая настройка Кентавра
- [ ] **Задача для AI**: Оптимизировать отображение Кентавра
  - [ ] **ЧАСТЬ 1**: Подбор идеального масштаба:
    - Кентавр не должен быть слишком большим/маленьким
    - Должен помещаться на экране полностью
    - Масштаб должен выглядеть естественно
  - [ ] **ЧАСТЬ 2**: Позиционирование:
    - Кентавр массивнее, может нужна позиция ниже
    - Проверить что не пересекается с UI элементами
    - Настроить центрирование
  - [ ] **ЧАСТЬ 3**: Скорость анимаций:
    - Кентавр может двигаться медленнее (более массивный)
    - Настроить `animationSpeeds` в конфиге
    - Анимация атаки может быть медленнее но мощнее

#### 🔍 **ПРОВЕРКА МИКРОШАГА 7.5:**
- [ ] Кентавр выглядит пропорционально и красиво
- [ ] Анимации проигрываются с подходящей скоростью
- [ ] Герой не выходит за границы экрана
- [ ] Переключение между героями работает плавно
- [ ] Масштаб адаптируется под разные размеры экрана

### ✅ Микрошаг 7.6 - Финальное тестирование системы героев
- [ ] **Задача для AI**: Комплексное тестирование системы
  - [ ] **ЧАСТЬ 1**: Тестирование всех анимаций:
    - Idle анимация работает корректно для обоих героев
    - Run анимация проигрывается при движении
    - Attack анимация срабатывает при атаке крипов
  - [ ] **ЧАСТЬ 2**: Проверка производительности:
    - Переключение героев не вызывает лагов
    - Загрузка ресурсов оптимальна
    - Нет утечек памяти при смене героев
  - [ ] **ЧАСТЬ 3**: Подготовка к расширению:
    - Система готова к добавлению новых героев
    - Код чистый и документированный
    - Конфигурация легко расширяема

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 7:**
**Что должно работать после выполнения всех шагов Фазы 7:**
- [ ] Запустить игру `/game`
- [ ] По умолчанию загружается Джаггернаут
- [ ] Консоль: `switchToCentaur()` - герой меняется на Кентавра
- [ ] Консоль: `switchToJuggernaut()` - герой меняется обратно
- [ ] Оба героя имеют разные масштабы и позиции
- [ ] Все анимации (idle, run, attack) работают для обоих героев
- [ ] При атаке крипов герой анимируется правильно
- [ ] Переключение происходит без ошибок и лагов
- [ ] Система готова к добавлению новых героев
- **✅ Если можно легко переключаться между Джаггернаутом и Кентавром в игре - Фаза 7 завершена!**

---

## 📋 Фаза 8: Система выбора героев (UI)

### ✅ Микрошаг 8.1 - Основа системы множественных героев
- [ ] **Задача для AI**: Создать систему управления несколькими героями
  - [ ] **ЧАСТЬ 1**: Создать `src/game/systems/HeroManager.ts`:
    ```typescript
    interface HeroData {
      id: string;                    // "juggernaut", "pudge", "invoker"
      name: string;                  // "Джаггернаут"
      level: number;                 // Уровень героя (1-30)
      gold: number;                  // Золото героя
      stats: HeroStats;              // Характеристики
      isUnlocked: boolean;           // Разблокирован ли
    }
    
    class HeroManager {
      getCurrentHero(): HeroData;
      getAllHeroes(): HeroData[];
      switchHero(heroId: string): void;
      unlockHero(heroId: string): void;
    }
    ```
  - [ ] **ЧАСТЬ 2**: Конфигурация начальных героев:
    - 3 героя доступны с начала (на выбор)
    - Остальные разблокируются при достижении 30 уровня
  - [ ] **ЧАСТЬ 3**: Сохранение данных всех героев в localStorage

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.1:**
- [ ] Консоль: `heroManager.getAllHeroes()` возвращает 3 разблокированных героя
- [ ] `switchHero("pudge")` переключает активного героя
- [ ] Данные сохраняются и восстанавливаются после перезагрузки

### ✅ Микрошаг 8.2 - UI выбора героя
- [ ] **Задача для AI**: Создать модальное окно выбора героя
  - [ ] **ЧАСТЬ 1**: Компонент `src/features/ui/HeroSelectionModal/HeroSelectionModal.tsx`:
    - Сетка доступных героев
    - Превью героя с характеристиками
    - Индикация заблокированных героев
    - Кнопка "Выбрать героя"
  - [ ] **ЧАСТЬ 2**: Интеграция с главной страницей:
    - Кнопка "Сменить героя" на главной странице
    - Показ текущего выбранного героя
  - [ ] **ЧАСТЬ 3**: Анимации переключения героя

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.2:**
- [ ] На главной странице есть кнопка "Сменить героя"
- [ ] При клике открывается модальное окно с сеткой героев
- [ ] Можно выбрать любого из 3 доступных героев
- [ ] Заблокированные герои показываются серыми
- [ ] После выбора анимация героя и характеристики обновляются

### ✅ Микрошаг 8.3 - Интеграция с игровым процессом
- [ ] **Задача для AI**: Полная интеграция системы героев с игрой
  - [ ] **ЧАСТЬ 1**: Модификация GameController для работы с HeroManager:
    - Загрузка характеристик текущего героя
    - Сохранение прогресса для конкретного героя
    - Синхронизация золота по героям
  - [ ] **ЧАСТЬ 2**: Разблокировка новых героев при достижении 30 уровня:
    - Специальное модальное окно "Новый герой разблокирован!"
    - Возможность сразу переключиться или остаться с текущим
  - [ ] **ЧАСТЬ 3**: Система достижений "Перерождение":
    - При достижении 30 уровня герой может "переродиться" (начать с 1 уровня)
    - Сохранение истории максимальных уровней

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 7 (ФИНАЛЬНАЯ):**
**Что должно быть видно на экране после выполнения всех шагов Фазы 7:**
- [ ] Начать игру - доступно 3 героя на выбор
- [ ] Выбрать героя, прокачать его до высокого уровня
- [ ] Переключиться на другого героя - у него свой отдельный прогресс и золото
- [ ] Прокачать первого героя до 30 уровня
- [ ] Должно появиться уведомление "Новый герой разблокирован!"
- [ ] В списке героев появляется 4-й разблокированный герой
- [ ] Система корректно сохраняет прогресс всех героев
- [ ] Можно играть за любого героя независимо
- **✅ Если система множественных героев работает полностью - ВСЯ СИСТЕМА ЗАВЕРШЕНА!**

---

## 🎯 **ДОПОЛНИТЕЛЬНЫЕ КОНТРОЛЬНЫЕ ТОЧКИ:**

### 🕐 **Быстрая проверка после каждого микрошага:**
- **После 1.1**: В консоли браузера не должно быть ошибок загрузки новых спрайтов
- **После 1.2**: Крип должен корректно создаваться с новым типом
- **После 2.1**: При перезагрузке должны появляться разные крипы
- **После 3.1**: В консоли должны логироваться убийства крипов со счетчиком
- **После 3.2**: Должен появляться увеличенный босс
- **После 4.1**: В localStorage должен сохраняться уровень героя
- **После 4.2**: Должна появляться модалка после убийства босса

### ⚠️ **Если что-то не работает:**
1. Проверить консоль браузера на ошибки
2. Убедиться что все файлы спрайтов находятся в папке `/public/media/game/assets/creeps/`
3. Проверить что пути к файлам в коде совпадают с реальными путями
4. Перезагрузить страницу и очистить кэш браузера

---

## 🎯 Приоритеты выполнения (НОВАЯ КОНЦЕПЦИЯ)

### 🥇 Поэтапная реализация (делаем по порядку)
1. **Фаза 1** - Добавление новых крипов (1.1 → 1.2 → 1.3) ✅ **ЗАВЕРШЕНА**
2. **Фаза 2** - Рандомизация крипов (2.1 → 2.2 → 2.3) ✅ **ЗАВЕРШЕНА**
3. **Фаза 3** - Основы уровневой системы (3.1 → 3.2 → 3.3) ✅ **ЗАВЕРШЕНА**
4. **Фаза 4** - Прогрессия крипов и боссов (4.1 → 4.2) ✅ **ЗАВЕРШЕНА**
5. **Фаза 4.5** - Улучшение визуального оформления HUD уровня (4.5.1 → 4.5.2 → 4.5.3) ⚡ **ТЕКУЩАЯ ФАЗА**
6. **Фаза 5** - Модальные окна и переходы (5.1 → 5.2 → 5.3)
7. **Фаза 6** - Главная страница с героем (6.1 → 6.2 → 6.3)
8. **Фаза 7** - Система выбора героев (7.1 → 7.2 → 7.3)

### 🎯 Ключевые особенности новой концепции
- **30 уровней героя = 30 уровней игры** (прямая связь 1:1)
- **Каждый уровень = 9 обычных крипов + 1 босс** (четкая структура)
- **Прогрессия крипов**: побежденный босс становится обычным крипом на следующем уровне
- **Главная страница**: анимированный герой + красивые характеристики
- **Система героев**: несколько героев с отдельным прогрессом

### 🎯 Философия: наглядность на каждом шаге
- После каждого микрошага должен быть видимый результат
- Тестируем каждую фичу сразу после реализации  
- Не переходим к следующему шагу пока не убедились что текущий работает
- **Возможность проверки после каждого микрошага или фазы**

---

## 📝 Технические детали крипов

### Спрайтшиты крипов (путь: `public/media/game/assets/creeps/`)

**medved:**
- `medved_attack.png` - 5x5 grid, 21 кадр
- `medved_death.png` - 5x5 grid, 21 кадр  
- `medved_idle.png` - 5x6 grid, 27 кадров

**satyr:**
- `satyr_attack.png` - 5x5 grid, 23 кадра
- `satyr_death.png` - 5x6 grid, 30 кадров
- `satyr_idle.png` - 6x7 grid, 40 кадров

**shishka:**
- `shishka_attack.png` - 5x6 grid, 27 кадров
- `shishka_death.png` - 5x6 grid, 27 кадров
- `shishka_idle.png` - 5x5 grid, 24 кадра

**voul:**
- `voul_attack.png` - 5x5 grid, 25 кадров
- `voul_death.png` - 4x8 grid, 31 кадр
- `voul_idle.png` - 7x5 grid, 34 кадра

**wolf:**
- `wolf_attack.png` - 3x7 grid, 21 кадр
- `wolf_death.png` - 3x6 grid, 18 кадров
- `wolf_idle.png` - 5x5 grid, 23 кадра

### Принципы работы (НОВАЯ КОНЦЕПЦИЯ)
- **Один уровень = 9 обычных крипов + 1 босс** (всегда 10 крипов на уровень)
- **Прошел уровень = повысил уровень героя** (1:1 соответствие)
- **Не прошел = остался на том же уровне** (можно повторить)
- **Максимум 30 уровней героя = 30 уровней игры**
- **Прогрессия**: каждый новый босс становится обычным крипом
- **Все настройки в коде** (не визуальная админка, легко настраивается)

---

## 📋 Техническая архитектура новой системы

### 🔧 Ключевые компоненты для реализации:

**HeroLevelSystem.ts** (Фаза 3):
```typescript
class HeroLevelSystem {
  private currentLevel: number = 1;
  private maxLevel: number = 30;
  
  getCurrentLevel(): number;
  levelUp(): void;
  getLevelName(): string; // Бронза/Серебро/Платина...
  canLevelUp(): boolean;
}
```

**LevelsConfig.ts** (Фаза 4):
```typescript
interface LevelConfig {
  level: number;
  normalCreeps: string[];     // ["satyr", "wolf"]
  bossCreep: string;         // "shishka"
  newCreepIntroduced?: string;
}

const LEVELS_CONFIG: LevelConfig[] = [
  { level: 1, normalCreeps: ["satyr", "wolf"], bossCreep: "shishka" },
  { level: 2, normalCreeps: ["satyr", "wolf", "shishka"], bossCreep: "direCreep" },
  // ... до 30 уровня
];
```

**HeroManager.ts** (Фаза 7):
```typescript
interface HeroData {
  id: string;
  name: string;
  level: number;
  gold: number;
  isUnlocked: boolean;
}

class HeroManager {
  private heroes: Map<string, HeroData>;
  private currentHeroId: string;
  
  switchHero(heroId: string): void;
  getCurrentHero(): HeroData;
  unlockHero(heroId: string): void;
}
```

### 🎯 Модальные окна для UX:
- **LevelCompleteModal** - после завершения уровня
- **NewCreepIntroModal** - знакомство с новым боссом  
- **HeroSelectionModal** - выбор героя
- **HeroUnlockModal** - разблокировка нового героя

### 📱 UI компоненты:
- **LevelProgressHUD** - прогресс уровня в игре
- **HeroStatsDisplay** - характеристики на главной странице
- **MainPageHeroAnimation** - анимированный герой

---

## 🚀 Готов начать?

**Текущий этап**: ✅ Фазы 1-2 завершены, переходим к **Фазе 3 - Основы уровневой системы**

**Следующий шаг**: Микрошаг 3.1 - Создание HeroLevelSystem.ts

### 🔥 Почему именно такая последовательность?
1. ✅ **Сначала крипы** - добавили всех, видим как выглядят
2. ✅ **Потом рандом** - сделали игру разнообразнее
3. ⚡ **Теперь уровни** - создаем систему 30 уровней героя 
4. 🔄 **Затем прогрессия** - крипы становятся доступными по мере прохождения
5. 🎨 **Потом модалки** - красивые переходы между уровнями
6. 🏠 **Главная страница** - показ героя и характеристик  
7. 👥 **Система героев** - несколько героев с отдельным прогрессом

### 💡 Преимущества новой концепции:
- **Четкая структура**: всегда понятно сколько крипов осталось
- **Прогрессия**: каждый уровень добавляет нового противника
- **Мотивация**: видимый прогресс от 1 до 30 уровня
- **Реиграбельность**: несколько героев с отдельным прогрессом

---

---

# 🎵 ФАЗА 8: Звуковая система (Audio System)

## 📋 Анализ звуков из старого проекта

### 🎯 **СТРУКТУРА ЗВУКОВ В СТАРОМ ПРОЕКТЕ:**

#### **🦸 Звуки героев** (`/media/game/sounds/heroes/juggernaut/`):
- **Бег**: `run/Jugger_run.mp3` - воспроизводился в цикле при анимации "run"
- **Атаки**: `attack/jugger_attack_0.mp3` до `jugger_attack_5.mp3` - случайный выбор при атаке

#### **👹 Звуки крипов** (`/media/game/sounds/creeps/`):
- **Атаки**:
  - `attack/attack_start_1.mp3` до `attack_start_8.mp3` - в начале анимации атаки (swingFrame)
  - `attack/attack_end_1.mp3` до `attack_end_12.mp3` - в конце анимации атаки (attackFrame)
- **Смерть**: По одному файлу для каждого типа крипа (`death/creep_death_1.mp3` до `death/creep_death_10.mp3`)

#### **🛒 Звуки магазина** (`/media/shop/sounds/`):
- **Покупка**: `buy.wav` - при покупке предмета
- **Продажа**: `sell.wav` - при продаже предмета

#### **🎵 UI звуки** (`/media/main/sounds/`):
- **Клик**: `click.wav` - звук клика по интерфейсу

### 🔍 **КАК ИСПОЛЬЗОВАЛИСЬ ЗВУКИ В СТАРОМ ПРОЕКТЕ:**

#### **Герой (hero.js):**
```javascript
// При атаке героя (случайный выбор)
const attackSounds = [
  new Audio('/media/game/sounds/heroes/juggernaut/attack/jugger_attack_0.mp3'),
  new Audio('/media/game/sounds/heroes/juggernaut/attack/jugger_attack_1.mp3'),
  // ... до jugger_attack_5.mp3
];
attackSounds[Math.floor(Math.random() * attackSounds.length)].play();

// При беге (цикл)
const runSound = new Audio('/media/game/sounds/heroes/juggernaut/run/Jugger_run.mp3');
runSound.loop = true;
runSound.play();
```

#### **Крипы (creep.js):**
```javascript
// При начале атаки крипа (swingFrame)
const attackStartSounds = [
  new Audio('/media/game/sounds/creeps/attack/attack_start_1.mp3'),
  // ... до attack_start_8.mp3
];

// При нанесении урона (attackFrame)
const attackEndSounds = [
  new Audio('/media/game/sounds/creeps/attack/attack_end_1.mp3'),
  // ... до attack_end_12.mp3
];

// При смерти крипа
const deathSound = new Audio(`/media/game/sounds/creeps/death/creep_death_${this.creepType}.mp3`);
deathSound.play();
```

#### **Магазин (shop.js):**
```javascript
// При покупке предмета
const buySound = new Audio('/media/shop/sounds/buy.wav');
buySound.play();

// При продаже предмета
const sellSound = new Audio('/media/shop/sounds/sell.wav');
sellSound.play();
```

#### **UI звуки (main.js):**
```javascript
// При клике по интерфейсу
const clickSound = new Audio('/media/main/sounds/click.wav');
clickSound.play();
```

---

## 🎯 План интеграции звуков в новую игру

### ✅ Микрошаг 8.1 - Создание системы управления звуками
- [x] **Задача для AI**: Создать `src/game/managers/AudioManager.ts`
  - [x] **ЧАСТЬ 1**: Базовая архитектура звуковой системы:
    ```typescript
    class AudioManager {
      private sounds: Map<string, HTMLAudioElement[]> = new Map();
      private isMuted: boolean = false;
      private volume: number = 0.5;
      
      // Основные методы
      loadSound(key: string, path: string): void;
      loadSoundArray(key: string, basePath: string, count: number): void;
      playSound(key: string, loop?: boolean): void;
      playRandomSound(key: string): void;
      stopSound(key: string): void;
      stopAllSounds(): void;
      
      // Управление громкостью
      setVolume(volume: number): void;
      setMuted(muted: boolean): void;
      
      // Создание звуковых групп
      createSoundGroup(groupKey: string, soundKeys: string[]): void;
    }
    ```
  - [x] **ЧАСТЬ 2**: Предзагрузка всех звуков:
    ```typescript
    // Звуки героя
    loadSoundArray('hero_attack', '/media/game/sounds/heroes/juggernaut/attack/jugger_attack_', 6);
    loadSound('hero_run', '/media/game/sounds/heroes/juggernaut/run/Jugger_run.mp3');
    
    // Звуки крипов
    loadSoundArray('creep_attack_start', '/media/game/sounds/creeps/attack/attack_start_', 8);
    loadSoundArray('creep_attack_end', '/media/game/sounds/creeps/attack/attack_end_', 12);
    loadSoundArray('creep_death', '/media/game/sounds/creeps/death/creep_death_', 10);
    
    // Звуки магазина
    loadSound('shop_buy', '/media/shop/sounds/buy.wav');
    loadSound('shop_sell', '/media/shop/sounds/sell.wav');
    
    // UI звуки
    loadSound('ui_click', '/media/main/sounds/click.wav');
    ```
  - [x] **ЧАСТЬ 3**: Интеграция с настройками:
    ```typescript
    // Сохранение настроек звука в localStorage
    saveAudioSettings(): void;
    loadAudioSettings(): void;
    
    // Интеграция с SettingsModal
    getVolume(): number;
    getMuted(): boolean;
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.1:**
- [x] Консоль браузера: `audioManager.playSound('hero_attack')` воспроизводит звук атаки
- [x] `audioManager.playRandomSound('hero_attack')` воспроизводит случайный звук атаки
- [x] `audioManager.setVolume(0.3)` изменяет громкость всех звуков
- [x] `audioManager.setMuted(true)` отключает все звуки
- [x] Настройки звука сохраняются в localStorage

### ✅ Микрошаг 8.2 - Интеграция звуков героя
- [x] **Задача для AI**: Добавить звуки в `src/game/entities/Hero.ts`
  - [x] **ЧАСТЬ 1**: Звуки атаки героя:
    ```typescript
    // В методе attack() или dealDamage()
    private playAttackSound(): void {
      if (this.audioManager) {
        this.audioManager.playRandomSound('hero_attack');
      }
    }
    ```
  - [x] **ЧАСТЬ 2**: Звуки бега героя:
    ```typescript
    // При запуске анимации бега
    private startRunSound(): void {
      if (this.audioManager) {
        this.audioManager.playSound('hero_run', true); // loop = true
      }
    }
    
    // При остановке анимации бега
    private stopRunSound(): void {
      if (this.audioManager) {
        this.audioManager.stopSound('hero_run');
      }
    }
    ```
  - [x] **ЧАСТЬ 3**: Управление состоянием звуков:
    ```typescript
    // Интеграция с анимационными состояниями
    private updateSoundState(): void {
      const currentAnimation = this.getCurrentAnimation();
      
      if (currentAnimation === 'run' && !this.isRunSoundPlaying) {
        this.startRunSound();
        this.isRunSoundPlaying = true;
      } else if (currentAnimation !== 'run' && this.isRunSoundPlaying) {
        this.stopRunSound();
        this.isRunSoundPlaying = false;
      }
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.2:**
- [x] В игре `/game`: при атаке героя должен воспроизводиться случайный звук атаки
- [x] При беге героя должен воспроизводиться цикличный звук бега
- [x] При остановке героя звук бега должен прекращаться
- [x] Разные атаки должны воспроизводить разные звуки (из 6 вариантов)

### ✅ Микрошаг 8.3 - Интеграция звуков крипов
- [x] **Задача для AI**: Добавить звуки в `src/game/entities/Creep.ts`
  - [x] **ЧАСТЬ 1**: Звуки атаки крипов:
    ```typescript
    // При начале анимации атаки (swingFrame)
    private playAttackStartSound(): void {
      if (this.audioManager) {
        this.audioManager.playRandomSound('creep_attack_start');
      }
    }
    
    // При нанесении урона (attackFrame)
    private playAttackEndSound(): void {
      if (this.audioManager) {
        this.audioManager.playRandomSound('creep_attack_end');
      }
    }
    ```
  - [x] **ЧАСТЬ 2**: Звуки смерти крипов:
    ```typescript
    // При смерти крипа
    private playDeathSound(): void {
      if (this.audioManager) {
        this.audioManager.playRandomSound('creep_death');
      }
    }
    ```
  - [x] **ЧАСТЬ 3**: Интеграция с анимационной системой:
    ```typescript
    // Вызов звуков в определенные кадры анимации
    private checkAnimationFrameForSound(): void {
      const currentFrame = this.getCurrentFrame();
      const animation = this.getCurrentAnimation();
      
      if (animation === 'attack') {
        if (currentFrame === this.swingFrame) {
          this.playAttackStartSound();
        } else if (currentFrame === this.attackFrame) {
          this.playAttackEndSound();
        }
      }
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.3:**
- [x] В игре `/game`: при атаке крипа должны воспроизводиться звуки в два этапа
- [x] При начале замаха - звук `attack_start`
- [x] При нанесении урона - звук `attack_end`
- [x] При смерти крипа - звук `creep_death`
- [x] Разные типы крипов должны воспроизводить разные звуки смерти

### ✅ Микрошаг 8.4 - Интеграция звуков магазина и UI
- [x] **Задача для AI**: Добавить звуки в магазин и UI элементы
  - [x] **ЧАСТЬ 1**: Звуки магазина в `src/pages/ShopPage/ShopPage.tsx`:
    ```typescript
    // При покупке предмета
    const handleBuyItem = (item: ShopItem) => {
      // ... логика покупки
      if (purchaseSuccessful) {
        audioManager.playSound('shop_buy');
      }
    };
    
    // При продаже предмета (если есть)
    const handleSellItem = (item: ShopItem) => {
      // ... логика продажи
      audioManager.playSound('shop_sell');
    };
    ```
  - [x] **ЧАСТЬ 2**: Звуки UI элементов:
    ```typescript
    // В компонентах UI (Header, Footer, кнопки навигации)
    const handleButtonClick = () => {
      audioManager.playSound('ui_click');
      // ... логика кнопки
    };
    ```
  - [x] **ЧАСТЬ 3**: Интеграция с навигацией:
    ```typescript
    // В App.tsx или компонентах навигации
    const handleNavigation = (targetRoute: string) => {
      audioManager.playSound('ui_click');
      // ... логика навигации
    };
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.4:**
- [x] В магазине `/shop` при покупке предмета должен воспроизводиться звук покупки
- [x] При клике по кнопкам навигации должен воспроизводиться звук клика
- [x] При переходе между страницами должен воспроизводиться звук перехода
- [x] Все UI элементы должны иметь соответствующие звуки

### ✅ Микрошаг 8.5 - Интеграция с настройками игры
- [x] **Задача для AI**: Добавить управление звуком в SettingsModal
  - [x] **ЧАСТЬ 1**: Обновление `src/features/ui/SettingsModal/SettingsModal.tsx`:
    ```typescript
    // Состояние звука
    const [volume, setVolume] = useState(0.5);
    const [isMuted, setIsMuted] = useState(false);
    
    // Обработчики изменений
    const handleVolumeChange = (newVolume: number) => {
      setVolume(newVolume);
      audioManager.setVolume(newVolume);
    };
    
    const handleMuteToggle = (muted: boolean) => {
      setIsMuted(muted);
      audioManager.setMuted(muted);
    };
    ```
  - [x] **ЧАСТЬ 2**: UI элементы в модальном окне:
    ```typescript
    // Ползунок громкости
    <input
      type="range"
      min="0"
      max="1"
      step="0.1"
      value={volume}
      onChange={(e) => handleVolumeChange(parseFloat(e.target.value))}
    />
    
    // Чекбокс отключения звука
    <input
      type="checkbox"
      checked={isMuted}
      onChange={(e) => handleMuteToggle(e.target.checked)}
    />
    ```
  - [x] **ЧАСТЬ 3**: Сохранение настроек:
    ```typescript
    // Сохранение в localStorage при изменении
    useEffect(() => {
      audioManager.saveAudioSettings();
    }, [volume, isMuted]);
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.5:**
- [x] В настройках должен быть ползунок громкости (0-100%)
- [x] В настройках должен быть чекбокс "Отключить звук"
- [x] Изменение громкости должно сразу влиять на все звуки в игре
- [x] Отключение звука должно останавливать все звуки
- [x] Настройки должны сохраняться между сессиями

### ✅ Микрошаг 8.6 - Оптимизация и полировка звуковой системы
- [x] **Задача для AI**: Оптимизировать и отполировать звуковую систему
  - [x] **ЧАСТЬ 1**: Оптимизация загрузки звуков:
    ```typescript
    // Ленивая загрузка звуков
    private loadSoundOnDemand(key: string): Promise<HTMLAudioElement> {
      return new Promise((resolve) => {
        const audio = new Audio();
        audio.addEventListener('canplaythrough', () => resolve(audio));
        audio.src = this.soundPaths[key];
      });
    }
    
    // Предзагрузка только критичных звуков
    preloadCriticalSounds(): void {
      const criticalSounds = ['hero_attack', 'creep_attack_start', 'creep_death'];
      criticalSounds.forEach(key => this.loadSound(key));
    }
    ```
  - [x] **ЧАСТЬ 2**: Управление одновременными звуками:
    ```typescript
    // Лимит одновременных звуков
    private maxSimultaneousSounds: number = 8;
    private activeSounds: HTMLAudioElement[] = [];
    
    // Очистка завершенных звуков
    private cleanupFinishedSounds(): void {
      this.activeSounds = this.activeSounds.filter(sound => !sound.ended);
    }
    ```
  - [x] **ЧАСТЬ 3**: Обработка ошибок и fallback:
    ```typescript
    // Обработка ошибок загрузки
    private handleSoundLoadError(key: string, error: Error): void {
      console.warn(`Failed to load sound: ${key}`, error);
      this.sounds.delete(key); // Удаляем неработающий звук
    }
    
    // Проверка поддержки audio
    private isAudioSupported(): boolean {
      return typeof Audio !== 'undefined';
    }
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 8.6:**
- [x] Звуки загружаются без ошибок в консоли
- [x] Не более 8 звуков воспроизводятся одновременно
- [x] Завершенные звуки корректно очищаются из памяти
- [x] Система работает даже если некоторые звуки не загрузились
- [x] Производительность остается стабильной при длительной игре

---

## 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 8:**

### 🎵 **Полное тестирование звуковой системы:**

#### **Игра `/game`:**
- [x] **Звуки героя**: При атаке - случайный звук атаки (6 вариантов)
- [x] **Звуки героя**: При беге - цикличный звук бега
- [x] **Звуки крипов**: При начале атаки - звук замаха
- [x] **Звуки крипов**: При нанесении урона - звук удара
- [x] **Звуки крипов**: При смерти - звук смерти
- [x] **Разнообразие**: Разные типы крипов - разные звуки

#### **Магазин `/shop`:**
- [x] **Покупка**: При покупке предмета - звук покупки
- [x] **Продажа**: При продаже предмета - звук продажи (если есть)

#### **UI элементы:**
- [x] **Звуки навигации**: При клике по кнопкам навигации - звук клика
- [x] **Звуки переходов**: При переходе между страницами - звук перехода

#### **Настройки:**
- [x] **Ползунок громкости**: Изменение громкости влияет на все звуки
- [x] **Отключение звука**: Чекбокс полностью отключает все звуки
- [x] **Сохранение**: Настройки сохраняются между сессиями

#### **Производительность:**
- [x] **Нет ошибок**: Консоль браузера без ошибок загрузки звуков
- [x] **Плавность**: Игра работает плавно даже с множеством звуков
- [x] **Память**: Нет утечек памяти при длительной игре

### 🎯 **Техническая проверка:**
- [x] `audioManager.playSound('hero_attack')` - работает
- [x] `audioManager.setVolume(0.3)` - изменяет громкость
- [x] `audioManager.setMuted(true)` - отключает все звуки
- [x] localStorage содержит настройки звука
- [x] Все звуковые файлы загружаются корректно

---


## ✅ **ЗАКЛЮЧЕНИЕ ФАЗЫ 8:**

**Цель Фазы 8:** Создать полноценную звуковую систему, которая улучшает игровой опыт и делает игру более живой и захватывающей.

**Ключевые достижения:**
- ✅ **Полная звуковая система**: Управление загрузкой, воспроизведением и настройками
- ✅ **Интеграция со всеми элементами**: Герой, крипы, магазин, UI элементы
- ✅ **Пользовательские настройки**: Управление громкостью и отключение звука
- ✅ **Оптимизация**: Эффективная загрузка и управление памятью
- ✅ **Совместимость**: Работа с существующими системами игры

**Результат:** Игра с качественным звуковым оформлением, которое соответствует оригинальному проекту и улучшает пользовательский опыт.

---

## ✨ ИТОГО: Новый план готов к реализации!

**Следующий шаг**: Начинаем Фазу 7, Микрошаг 7.1 - создание системы конфигов героев

**🆕 ОБНОВЛЕНИЕ ПЛАНА:** Добавлена **Фаза 7** - система конфигурации героев и добавление Кентавра!
- ✅ **Цель**: Создать гибкую систему героев по аналогии с крипами
- ✅ **Результат**: Возможность переключения между Джаггернаутом и Кентавром в игре
- ✅ **Подход**: Конфигурационные файлы вместо хардкода, легко расширяемая система
- ✅ **Тестирование**: Переключение через консоль браузера `switchToCentaur()` / `switchToJuggernaut()`

Каждый микрошаг можно проверить, каждая фаза дает видимый результат! 🎯 

---

## 📋 Фаза 9: Обновление системы героев и джаггернаута

### ✅ Микрошаг 9.1 - Изменение структуры идентификаторов героев
- [ ] **Задача для AI**: Обновить структуру heroConfig.ts для работы с числовыми ID
  - [ ] **ПРОБЛЕМА**: Сейчас используются строковые `id: 'juggernaut'/'centaur'`, но API работает с числовыми hero_id
  - [ ] **РЕШЕНИЕ**: Добавить поля `hero_id: number` и `hero_name: string` в интерфейс HeroTypeConfig:
    ```typescript
    interface HeroTypeConfig {
      hero_id: number;        // 1, 2, 3... (для API)
      hero_name: string;      // "juggernaut", "centaur" (техническое имя)
      id: string;            // Оставляем для обратной совместимости
      name: string;          // "Джаггернаут", "Кентавр" (отображаемое имя)
      // ... остальные поля
    }
    ```
  - [ ] **ЧАСТЬ 1**: Обновить конфигурацию джаггернаута:
    ```typescript
    juggernaut: {
      hero_id: 1,
      hero_name: "juggernaut", 
      id: 'juggernaut',      // Сохраняем для совместимости
      name: 'Джаггернаут',
      // ... остальные поля
    }
    ```
  - [ ] **ЧАСТЬ 2**: Обновить конфигурацию кентавра:
    ```typescript
    centaur: {
      hero_id: 2,
      hero_name: "centaur",
      id: 'centaur',
      name: 'Кентавр Завоеватель',
      // ... остальные поля
    }
    ```
  - [ ] **ЧАСТЬ 3**: Добавить функции маппинга:
    ```typescript
    export function getHeroConfigByNumericId(heroId: number): HeroTypeConfig;
    export function getHeroNameByNumericId(heroId: number): string;
    export function mapNumericIdToHeroName(heroId: number): string; // 1 → "juggernaut"
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 9.1:**
- [ ] Консоль: `getHeroConfigByNumericId(1)` возвращает конфигурацию джаггернаута
- [ ] Консоль: `mapNumericIdToHeroName(1)` возвращает "juggernaut"
- [ ] Консоль: `mapNumericIdToHeroName(2)` возвращает "centaur"
- [ ] Старые функции (`getHeroConfig('juggernaut')`) продолжают работать

### ✅ Микрошаг 9.2 - Обновление конфигурации джаггернаута с новыми спрайтами
- [ ] **Задача для AI**: Заменить конфигурацию джаггернаута на AdaptiveSpriteSheetConfig с новыми параметрами
  - [ ] **ПРОБЛЕМА**: Текущая конфигурация джаггернаута использует старые спрайты и обычный SpriteSheetConfig
  - [ ] **РЕШЕНИЕ**: Обновить на AdaptiveSpriteSheetConfig с HD/MD/LD качествами
  - [ ] **ЧАСТЬ 1**: Заменить анимации idle, attack, run на новые параметры:
    ```typescript
    // Новые параметры из пользователя:
    juggernaut_attack: { hd: 1024x1024, 5x5, 23 кадра }
    juggernaut_run: { hd: 1024x1024, 5x4, 19 кадров }  
    juggernaut_idle: { hd: 1024x1024, 8x8, 62 кадра }
    // + MD (512x512) и LD (256x256) версии с теми же параметрами кроме размера
    ```
  - [ ] **ЧАСТЬ 2**: Добавить анимацию front для главной страницы:
    ```typescript
    juggernaut_front: { hd: 1024x1024, 8x8, 61 кадр }
    // + MD и LD версии
    ```
  - [ ] **ЧАСТЬ 3**: Обновить пути к файлам на новые:
    ```typescript
    // Новые пути согласно структуре пользователя:
    '/media/game/assets/heroes/juggernaut/juggernaut_attack/juggernaut_attack_hd.webp'
    '/media/game/assets/heroes/juggernaut/juggernaut_run/juggernaut_run_hd.webp'
    '/media/game/assets/heroes/juggernaut/juggernaut_idle/juggernaut_idle_hd.webp' 
    '/media/game/assets/heroes/juggernaut/juggernaut_front/juggernaut_front_hd.webp'
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 9.2:**
- [ ] В игре `/game` джаггернаут отображается с новыми спрайтами
- [ ] Все анимации (idle, attack, run) работают с правильными параметрами кадров
- [ ] Адаптивное качество работает - на слабых устройствах используется LD, на мощных HD
- [ ] Анимация front доступна но используется только на главной странице

### ✅ Микрошаг 9.3 - Реализация способности джаггернаута "Blade Dance"
- [ ] **Задача для AI**: Добавить способность критического удара в HeroAbilities.ts
  - [ ] **ЧАСТЬ 1**: Создать класс BladeDanceAbility:
    ```typescript
    export class BladeDanceAbility implements PassiveAbility {
      public readonly id = 'blade_dance';
      public readonly name = 'Blade Dance';
      public readonly description = 'Наносит критический урон в размере 130% с вероятностью 10%';
      public readonly heroType = 'juggernaut';
      
      private readonly CRIT_CHANCE = 10; // 10% вероятность
      private readonly CRIT_MULTIPLIER = 1.3; // 130% урона
    }
    ```
  - [ ] **ЧАСТЬ 2**: Реализовать onDealDamage логику:
    ```typescript
    public onDealDamage(context: DealDamageContext): AbilityResult {
      // Проверить вероятность критического удара (10%)
      const isCritical = Math.random() * 100 < this.CRIT_CHANCE;
      
      if (isCritical) {
        // Увеличить урон до 130%
        const criticalDamage = Math.floor(context.damage * this.CRIT_MULTIPLIER);
        const bonusDamage = criticalDamage - context.damage;
        
        // Нанести дополнительный урон крипу
        context.target.takeDamage(bonusDamage);
        
        // Создать визуальный эффект критического удара
        // Показать число урона красным цветом или с эффектом
        
        return {
          handled: true,
          modifiedValue: criticalDamage,
          effects: [{
            type: 'damage',
            target: context.target,
            value: bonusDamage,
            description: `Критический удар! +${bonusDamage} урона`
          }]
        };
      }
      
      return { handled: false };
    }
    ```
  - [ ] **ЧАСТЬ 3**: Интеграция иконки способности:
    ```typescript
    // Путь к иконке из структуры пользователя:
    icon: '/media/game/assets/heroes/juggernaut/juggernaut_abilities/juggernaut_blade_dance.png'
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 9.3:**
- [ ] В игре джаггернаут иногда (примерно в 10% случаев) наносит критический урон
- [ ] При критическом ударе отображается дополнительное число урона
- [ ] На главной странице отображается способность с иконкой и описанием
- [ ] Способность работает только у джаггернаута, не у кентавра

### ✅ Микрошаг 9.4 - Интеграция с TEST_HERO_ID и переключением героев
- [ ] **Задача для AI**: Создать систему переключения героев через TEST_HERO_ID
  - [ ] **ЧАСТЬ 1**: Обновить логику использования TEST_HERO_ID:
    ```typescript
    // В компонентах где используется TEST_HERO_ID
    import { TEST_HERO_ID } from '../../shared/constants';
    import { mapNumericIdToHeroName } from '../../game/config/heroConfig';
    
    // Получить имя героя для использования в игре
    const currentHeroName = mapNumericIdToHeroName(parseInt(TEST_HERO_ID));
    ```
  - [ ] **ЧАСТЬ 2**: Обновить GameController для использования переменного героя:
    ```typescript
    // При создании героя в GameController
    private createHero(): void {
      const heroId = parseInt(TEST_HERO_ID);
      const heroName = mapNumericIdToHeroName(heroId);
      this.hero = new Hero(heroName, /* остальные параметры */);
    }
    ```
  - [ ] **ЧАСТЬ 3**: Обновить MainPage для отображения текущего героя:
    ```typescript
    // В MainPage.tsx отображать героя согласно TEST_HERO_ID
    const currentHeroId = parseInt(TEST_HERO_ID);
    const heroConfig = getHeroConfigByNumericId(currentHeroId);
    
    // Использовать анимацию front для отображения
    <HeroDisplay heroType={heroConfig.hero_name} animation="front" />
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 9.4:**
- [ ] Изменение `TEST_HERO_ID` с "1" на "2" в constants/index.ts меняет героя в игре
- [ ] На главной странице отображается правильный герой согласно TEST_HERO_ID  
- [ ] В игре `/game` появляется правильный герой согласно TEST_HERO_ID
- [ ] Способности соответствуют выбранному герою (критический удар только у джаггернаута)

### ✅ Микрошаг 9.5 - API интеграция для смены героев
- [ ] **Задача для AI**: Интеграция с backend API для смены активного героя
  - [ ] **ЧАСТЬ 1**: Создать функцию смены героя:
    ```typescript
    // В src/shared/api/heroService.ts
    export async function switchActiveHero(userId: string, heroId: number): Promise<boolean> {
      try {
        const response = await fetch(`${API_BASE_URL}/update_user_money`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            user_id: parseInt(userId),
            hero_id: heroId
          })
        });
        return response.ok;
      } catch (error) {
        console.error('Failed to switch hero:', error);
        return false;
      }
    }
    ```
  - [ ] **ЧАСТЬ 2**: Получение активного героя из API:
    ```typescript
    // Модификация существующих функций для получения hero_id из ответа
    // GET /hero_data уже возвращает h.hero_id - использовать это значение
    ```
  - [ ] **ЧАСТЬ 3**: Интеграция с UI для переключения героев:
    ```typescript
    // На главной странице в слайдере героев
    const handleHeroSelect = async (heroId: number) => {
      const success = await switchActiveHero(TEST_USER_ID, heroId);
      if (success) {
        // Обновить TEST_HERO_ID в runtime или перезагрузить характеристики
        window.location.reload(); // Временное решение
      }
    };
    ```

#### 🔍 **ПРОВЕРКА МИКРОШАГА 9.5:**
- [ ] Клик по герою в слайдере на главной странице меняет активного героя
- [ ] API запрос POST /update_user_money отправляется с правильными параметрами
- [ ] После смены героя характеристики обновляются соответственно
- [ ] GET /hero_data возвращает правильный hero_id активного героя

### ✅ Микрошаг 9.6 - Тестирование и отладка системы героев
- [ ] **Задача для AI**: Комплексное тестирование обновленной системы героев
  - [ ] **ЧАСТЬ 1**: Проверка переключения героев:
    ```typescript
    // Глобальные функции для тестирования
    (window as any).testSwitchHero = (heroId: number) => {
      console.log(`Switching to hero ${heroId}`);
      // Логика смены героя
    };
    
    (window as any).testHeroAbilities = () => {
      // Проверка способностей текущего героя
    };
    ```
  - [ ] **ЧАСТЬ 2**: Проверка совместимости с существующими системами:
    - Магазин должен работать с новой структурой характеристик
    - Система золота должна корректно синхронизироваться
    - У каждого героя свой уровень
  - [ ] **ЧАСТЬ 3**: Проверка производительности:
    - Переключение героев не должно вызывать лагов
    - Новые спрайты должны загружаться оптимально
    - Адаптивное качество должно работать корректно

#### 🔍 **ПРОВЕРКА РЕЗУЛЬТАТА ФАЗЫ 9:**
**Что должно работать после выполнения всех шагов Фазы 9:**
- [ ] **Переключение героев**: Изменение TEST_HERO_ID меняет героя в игре и на главной странице
- [ ] **Новые спрайты джаггернаута**: Все анимации работают с новыми высококачественными спрайтами
- [ ] **Адаптивное качество**: На разных устройствах используются соответствующие версии (HD/MD/LD)
- [ ] **Анимация front**: На главной странице используется фронтальная анимация джаггернаута
- [ ] **Способность Blade Dance**: Джаггернаут наносит критический урон с вероятностью 10%
- [ ] **API интеграция**: Смена героя через интерфейс отправляет запросы на сервер
- [ ] **Полная совместимость**: Все существующие системы работают с новой структурой героев
- **✅ Если можно легко переключаться между джаггернаутом и кентавром, джаггернаут имеет новые спрайты и способность критического удара - Фаза 9 завершена!**

---

// ... existing code ...